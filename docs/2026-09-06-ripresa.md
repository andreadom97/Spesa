# Punto di ripresa — 06/09/2026

Scritto alla fine della sessione remota del 05/09 per riprendere il lavoro senza la
conversazione. Tutto ciò che conta è nel repo: questo file dice dove.

## Dove siamo

- **Branch:** `claude/food-waste-cost-reduction-547c7c`, 46 commit sopra `main`,
  pull request [andreadom97/Spesa#1](https://github.com/andreadom97/Spesa/pull/1)
  aperta il 05/09, non ancora mergiata.
- **Verifica dell'albero al 06/09:** 1003 test verdi (615 su `main`), `tsc`, `lint` e
  `build` puliti. `npm run eval:import` senza chiave esce con NON ESEGUITO.
- **CI attiva dal 06/09** (`.github/workflows/ci.yml`): test, build, tsc, lint su push a
  `main` e su ogni pull request.

## Cosa è stato deciso (in ordine)

1. **Niente pivot di target verso l'anti-spreco: pivot della promessa.** Il residuo
   derivato è già un motore anti-sovracquisto; "compri solo quello che manca e vedi
   quanto non hai buttato" si aggiunge a "porta la tua dieta". Il generalista resta
   fuori perché il residuo derivato è preciso solo con un piano rigido. Segmento
   naturale: chi cucina per uno o due; genere vago nel copy; niente retail media.
   Dettaglio in `spesa-backlog-nicchia.md`, "Conclusioni del 05/09".
2. **EasyPlan** è il concorrente di riferimento: app creator-led di Samantha Capola
   (Basilico Secco, ~306k su Instagram), costruita dalla sua agenzia Alchemica, su Play
   da settembre 2024, da 7,99 €/mese senza piano annuale, ricette supervisionate dalla
   nutrizionista Francesca Ferrone, dispensa manuale. Trazione modesta (13 recensioni
   Trustpilot, 15k follower dell'app). Non ha il residuo derivato. La SWOT è nella
   conversazione del 05/09, non in un file: se serve, va riscritta da questi fatti.
3. **Backlog rivisto** (`spesa-backlog-nicchia.md`): P0 e P1 (import in produzione) e
   P2 (contatore "non hai ricomprato") consegnati il 05/09; **P3 è la prossima**
   (avviso di scadenza del fresco), P4 (le due porte e l'inserimento veloce) e P5 (primo
   avvio automatico) e P6 (la casa condivisa, salvo "per quante persone": decisione
   aperta nella spec §6) consegnati il 06/09; **P7 è la prossima** (lettura offline
   della lista), poi P8 barcode, P9 store.
4. **Modello per l'import: Sonnet finché l'eval non dimostra che serve Opus**, con la
   regola di decisione scritta nella spec dell'import §4. Il limite di import per
   utente è una difesa dall'abuso, non una leva di costo.

## Cosa è stato costruito il 05/09

- **Import in produzione** — spec `docs/superpowers/specs/2026-09-05-import-in-produzione-design.md`,
  piano omonimo in `plans/`. Estrazione a pagine (indice → pagine in parallelo con
  cache → fusione in codice), PDF diviso con `pdf-lib`, tetto per utente su
  `import_uso` (migrazione 0010), eval con manifest e report con costi. Poi una
  review di correttezza e una di sicurezza, con le correzioni in un commit unico
  (`fix(import): correzioni dalla review…`).
- **Contatore "non hai ricomprato"** — spec `docs/superpowers/specs/2026-09-05-non-ricomprato-design.md`,
  piano omonimo. `evitato` in `costruisciLista`, prezzo per confezione
  sull'ingrediente (migrazione 0011, editor e passo formati dell'import), tabella
  `risparmio_settimana`, scheda in "Hai preso tutto", riga in Dispensa.
- README allineato (sezioni "Estrazione a pagine", "Tetto di import per utente",
  "Variabili d'ambiente dell'import", "Quanto non hai ricomprato", migrazioni nel
  deploy).

- **Il fresco che scade (P3, 06/09)** — spec `docs/superpowers/specs/2026-09-06-scadenza-fresco-design.md`,
  piano omonimo. `scadenzaResiduo`/`avvisiScadenza` (`src/domain/scadenza.ts`) e
  `conflittiSostituzione` (`src/domain/conflitto.ts`); righe in Settimana, Dispensa e
  Scegli; nota di Scegli corretta (il top-up si allinea da solo). Nessuna migrazione.

- **Le due porte e il primo avvio (P4 e P5, 06/09)** — spec
  `docs/superpowers/specs/2026-09-06-due-porte-design.md`, piano omonimo. Semina automatica
  nel layout (`PrimoAvvio` → `assicuraDatiIniziali`), i 71 ingredienti in
  `src/domain/ingredienti-base.ts`, stato vuoto di Piatti con le due porte, `/piatti/veloce`,
  Lista e Settimana vuote che rimandano a Piatti. Nessuna migrazione.

- **La casa condivisa (P6, 06/09)** — spec `docs/superpowers/specs/2026-09-06-casa-condivisa-design.md`,
  piano omonimo. Migrazione 0012 (`casa_id()`, tutte le policy rigenerate, cinque RPC),
  `src/data/casa.ts` e `idCasa()` in tutto il data layer e nella route dell'import,
  sezione CASA in Impostazioni (codice, ingresso, uscita, TOGLI), Lista che si rilegge al
  ritorno in primo piano.
- **Review del 06/09** (correttezza e sicurezza su P3, P4, P5): due alte corrette (il
  conflitto a settimana confermata contava due volte lo storno → ora usa il residuo
  congelato in lista; una settimana nata a repertorio vuoto restava vuota → ora
  `completaAssegnazioni` la ricompila), più timeout del cancello, chiavi, contatore per
  pasto, tetti agli input, `permissions` nel workflow. Un finding medio è un'impostazione
  del pannello Supabase: spegnere le iscrizioni aperte.

## Cose da sapere che non si deducono dal codice

- Per un PDF la riga in `import_uso` viene registrata con `pagine = 0` perché contarle
  significherebbe aprirlo prima del tetto (un PDF costruito ad arte esaurisce la
  memoria già in `load`). Il check SQL è `0..12`.
- Con l'ordine "registra, poi conta", la data nel 429 è "il più vecchio import nella
  finestra più 30 giorni" e il tentativo rifiutato resta nella finestra: alla data
  indicata può arrivare un altro 429. Imprecisione accettata.
- `IMPORT_CONCORRENZA=0` vale 1, non 4. `IMPORT_LIMITE_30GG` vuota vale 3.
- L'errore tsc su `LayoutProps` in `layout.tsx` compare solo senza `.next/types`: dopo
  un `npm run build` sparisce. Non è nostro.
- `diete/` è gitignored e contiene dati sanitari veri: mai in git, mai nei report. L'eval
  gira solo in locale con quella cartella e la chiave.
- Il file `scripts/eval-import-report.ts` era binario per git (byte NUL nel sorgente)
  fino al commit di correzione; ora è testo.
- Il cancello `PrimoAvvio` mostra le pagine solo dopo la semina (o dopo 4 s): aggiunge
  una RPC `casa_id` e due conteggi a ogni apertura dell'app. Se un giorno pesasse, la
  strada è un flag in `settings`.
- Lo storno di `aggiornaSlot` è attivo da settimana non bozza, cioè anche a confermata:
  chi ragiona sul residuo fra conferma e chiusura deve tenerne conto (è l'errore trovato
  in review nel conflitto di Scegli).
- Chi entra in una casa non vede più i propri dati finché non esce: non è un bug, è il
  disegno (spec casa §1).
- In Vitest 4 un `beforeEach(() => vi.mocked(fn).mockReset())` senza graffe restituisce
  il mock e il runner lo richiama come pulizia a fine test: innocuo finché `fn()` senza
  argomenti non rigetta. Con le graffe non succede (visto in `primo-avvio.test.tsx`).
- La prima chiamata di pagina dovrebbe leggere la cache scritta dall'indice: il numero
  `cache letti` nel report dell'eval è il primo da guardare. Se è zero, usare lo stesso
  schema di structured output per indice e pagine.

## Metodo di lavoro concordato

Spec di design in `docs/superpowers/specs/`, poi piano in `docs/superpowers/plans/`
con task, file, interfacce e passi TDD. Esecuzione **subagent-driven**: un subagent
per task, in parallelo quando i file sono disgiunti, i subagent **non committano**;
l'orchestratore rivede il diff, fa girare `npx vitest run && npx tsc --noEmit && npm run lint && npm run build`,
committa un task per volta con messaggio in italiano e pusha. Prima di una PR: una
review di correttezza e una di sicurezza in sola lettura, poi un subagent di
correzione con la lista consolidata. Commit con `Co-Authored-By: Claude Fable 5.1` e
`Claude-Session`. Codice, commenti, test e documenti in italiano.

## Variabili d'ambiente dell'import

`ANTHROPIC_API_KEY` (accende l'estrazione), `ANTHROPIC_WORKSPACE_ID` (opzionale),
`IMPORT_AI_MODEL` (default `claude-sonnet-5`), `IMPORT_AI_EFFORT` (opzionale, `low`
consigliato con Opus), `IMPORT_LIMITE_30GG` (default 3, `0` disattiva),
`IMPORT_CONCORRENZA` (default 4), `IMPORT_MOCK` (solo sviluppo). Per l'eval:
`EVAL_IMPORT_MODELLI`, `EVAL_IMPORT_PIPELINE` (`pagine`, `singola`, `entrambe`),
`EVAL_IMPORT_SET` (`originali`, `compresse`, `entrambi`), `EVAL_IMPORT_DIR_FOTO`,
`EVAL_IMPORT_DEBUG`.

## Da fare in locale (non fattibile da remoto)

1. **Supabase**: il progetto sta per essere messo in pausa per inattività. Qualunque
   richiesta lo tiene attivo; se si ferma, si ripristina dal pannello. Prima del deploy
   del branch applicare dall'SQL Editor, in ordine, **0010, 0011 e 0012**. Le prime due
   sono sicure anche con il codice di `main` in produzione (una tabella nuova, una
   colonna nullable e una tabella nuova) e contano come attività; la **0012** sostituisce
   tutte le policy RLS con `casa_id()` ed è sicura per un utente solo (casa = sé stesso),
   ma senza di lei il codice del branch non carica. In Auth, spegnere le iscrizioni
   aperte ("Allow new users to sign up") finché l'app è per persone note.
2. Spend limit mensile sul workspace Anthropic.
3. `diete/eval-manifest.json` e la cartella delle foto compresse; poi
   `EVAL_IMPORT_MODELLI=claude-sonnet-5,claude-opus-5 EVAL_IMPORT_PIPELINE=entrambe npm run eval:import`
   e leggere `diete/estrazioni/report-*.md`. Decidere il modello con la regola della
   spec §4.
4. Su Vercel `ANTHROPIC_API_KEY`, più le altre variabili solo se diverse dai default.
   Redeploy.
5. Import della dieta 6 dal telefono con il cronometro (criterio: sotto i 150 s). Con
   un secondo account, il quarto import in 30 giorni deve dare il 429.
6. Con un secondo account nuovo: al primo accesso pasti e 71 ingredienti devono esserci
   da soli; da Piatti le due porte; da `/piatti/veloce` otto piatti in pochi minuti.
6b. Un deperibile con residuo (dopo una chiusura): in Dispensa deve comparire `SCADE IL …`;
   se nessun pasto lo usa in tempo, la riga sotto; in Settimana l'avviso sul pasto dopo.
7. Un prezzo a una decina di ingredienti ricorrenti; alla prossima lista guardare "Hai
   preso tutto", e dopo la chiusura la riga in Dispensa.
8. Con due account: codice dal primo, ingresso dal secondo, spunta da entrambi, TOGLI e
   uscita. Decidere su "per quante persone" (spec casa §6).
9. Mergiare la PR quando le prove tornano.

## Cosa si può ancora fare da remoto, in ordine consigliato

1. ~~CI su GitHub Actions~~, ~~P3~~, ~~P4~~, ~~P5~~: fatte il 06/09.
2. ~~P6~~ fatta il 06/09 (salvo la decisione aperta), ~~review di P3/P4/P5~~ fatta.
3. **P7**: lettura offline della lista (il guscio c'è, i dati no).
4. Una review di correttezza e sicurezza sui commit di P6 (migrazione 0012 in testa),
   prima del merge.
5. Le due landing del fake door ("porta la tua dieta" contro "compri solo quello che
   manca") e le interviste sono validazione, non codice: restano le cose più
   importanti mai eseguite.
