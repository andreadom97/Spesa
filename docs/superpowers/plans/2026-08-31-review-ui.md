# Review UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Allineare navigazione, gesti e coerenza dell'app alla sua dimensione reale: tab bar a 4 voci, identità vera, vista/modifica sul piatto, Dispensa riordinata, lotto copy/accessibilità, animazioni causa-effetto.

**Architecture:** Solo presentazione: componenti React esistenti (inline styles, pattern del progetto), zero cambi a dominio/dati/API. Due lotti: Lotto 1 = struttura (Task 1-7), Lotto 2 = coerenza+animazioni (Task 8-10). Il Lotto 2 non parte se il Lotto 1 non è verde (suite + giro browser).

**Tech Stack:** Next.js 16 App Router, React 19, Vitest 4 + Testing Library, CSS inline + globals.css.

**Spec:** `docs/superpowers/specs/2026-08-31-review-ui-design.md` — contiene l'ERRATA sull'audit (sezione B): i gesti della card pasto sono GIÀ corretti, restano solo le rifiniture.

## Global Constraints

- Zero modifiche a `src/domain/**` (eccetto nessuna), `src/data/**`, `src/server/**`, API routes, tipi condivisi. Solo componenti, pagine, copy, CSS.
- Dark mode FUORI scope. Selettore settimane FUORI scope (si rimuove solo la freccetta).
- Animazioni: solo CSS, 150–250ms, tutte dentro `@media (prefers-reduced-motion: no-preference)`. Vietate animazioni di ingresso pagina.
- Copy esatti dalla spec: title `"Spesa"`, description `"La spesa e la settimana della tua dieta: piano, lista e dispensa che si tengono aggiornati da soli."`, voce foglio `"Ho mangiato fuori piano"`, riga spiegazione dispensa `"Calcolato da spesa e piano: correggi solo se non torna con la realtà."`, bottone import `"SCEGLI LE FOTO"`.
- Ogni task: suite verde (`npm test`), poi commit. Stile del codice: commenti e nomi in italiano, pattern inline-style del file toccato.
- I test di componente esistenti usano `@testing-library/react` + `vitest` (vedi `src/components/__tests__/` e `src/app/(app)/importa/__tests__/camera.test.tsx` come riferimento di stile: `beforeEach` con `vi.restoreAllMocks()`, query per ruolo/nome accessibile).

---

## Lotto 1 — struttura

### Task 1: Identità e root

**Files:**
- Modify: `src/app/layout.tsx:5-9,17`
- Modify: `src/app/page.tsx` (riscrittura completa, 7 righe)
- Test: `src/app/__tests__/identita.test.tsx` (nuovo)

**Interfaces:**
- Consumes: niente.
- Produces: `/` che reindirizza a `/lista`; `metadata.title === 'Spesa'`. Nessun altro task dipende da questo.

- [ ] **Step 1: Test che fallisce**

```tsx
// src/app/__tests__/identita.test.tsx
import { describe, it, expect } from 'vitest';
import { metadata } from '../layout';
import Home from '../page';

describe('identità', () => {
  it('il title è Spesa, non il default del template', () => {
    expect(metadata.title).toBe('Spesa');
    expect(String(metadata.description)).not.toMatch(/create next app/i);
  });

  it('la root reindirizza a /lista', () => {
    // redirect() di next/navigation lancia un errore con digest NEXT_REDIRECT;<tipo>;<url>;...
    let digest = '';
    try {
      Home();
    } catch (err) {
      digest = (err as { digest?: string }).digest ?? '';
    }
    expect(digest).toContain('NEXT_REDIRECT');
    expect(digest).toContain('/lista');
  });
});
```

- [ ] **Step 2: Run — atteso FAIL** (`npx vitest run src/app/__tests__/identita.test.tsx`: title è "Create Next App", Home non lancia).

- [ ] **Step 3: Implementazione**

In `src/app/layout.tsx`:

```tsx
export const metadata: Metadata = {
  title: "Spesa",
  description: "La spesa e la settimana della tua dieta: piano, lista e dispensa che si tengono aggiornati da soli.",
  manifest: "/manifest.json",
};
```

e `<html lang="it" ...>` al posto di `lang="en"` (riga 17).

`src/app/page.tsx`, riscritto:

```tsx
import { redirect } from 'next/navigation';

/** La root non ha contenuto suo: l'app comincia dalla Lista (come il manifest PWA). */
export default function Home() {
  redirect('/lista');
}
```

- [ ] **Step 4: Run — atteso PASS**, poi `npm test` completo.

- [ ] **Step 5: Commit**

```bash
git add src/app/layout.tsx src/app/page.tsx src/app/__tests__/identita.test.tsx
git commit -m "fix(identita): title e description veri, lang it, root che reindirizza a /lista"
```

### Task 2: Tab bar a 4 voci

**Files:**
- Modify: `src/components/TabBar.tsx:6-38` (VOCI) e commento riga 40
- Modify: `src/app/(app)/impostazioni/page.tsx:341-347` circa (rimozione blocco `Etichetta DISPENSA` + `Link href="/dispensa"`)
- Test: `src/components/__tests__/tabbar.test.tsx` (nuovo o esteso se esiste)

**Interfaces:**
- Consumes: pattern VOCI esistente in TabBar.tsx (array `{href, etichetta, icona}`).
- Produces: quarta voce `{href: '/dispensa', etichetta: 'DISPENSA'}`. Nessuna firma nuova.

- [ ] **Step 1: Test che fallisce**

```tsx
// src/components/__tests__/tabbar.test.tsx
import '@testing-library/jest-dom/vitest';
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';

const percorso = vi.hoisted(() => ({ valore: '/lista' }));
vi.mock('next/navigation', () => ({ usePathname: () => percorso.valore }));

import { TabBar } from '../TabBar';

describe('TabBar', () => {
  it('ha quattro voci: LISTA, SETTIMANA, PIATTI, DISPENSA', () => {
    render(<TabBar />);
    for (const nome of ['LISTA', 'SETTIMANA', 'PIATTI', 'DISPENSA']) {
      expect(screen.getByRole('link', { name: new RegExp(nome) })).toBeInTheDocument();
    }
  });

  it('DISPENSA è attiva su /dispensa', () => {
    percorso.valore = '/dispensa';
    render(<TabBar />);
    const voce = screen.getByRole('link', { name: /DISPENSA/ });
    expect(voce.getAttribute('href')).toBe('/dispensa');
  });
});
```

- [ ] **Step 2: Run — atteso FAIL** (DISPENSA non esiste).

- [ ] **Step 3: Implementazione** — quarta voce in coda a `VOCI` (icona barattolo/scaffale nello stile line-art 21×21, stroke 1.7-1.9 come le altre):

```tsx
  {
    href: '/dispensa',
    etichetta: 'DISPENSA',
    icona: (colore: string) => (
      <svg width="21" height="21" viewBox="0 0 24 24" fill="none">
        <path d="M8 4.5h8M9.5 4.5v-1h5v1" stroke={colore} strokeWidth="1.7" strokeLinecap="round" />
        <rect x="6.5" y="6.5" width="11" height="14" rx="2.5" stroke={colore} strokeWidth="1.8" />
        <path d="M6.5 12h11" stroke={colore} strokeWidth="1.7" strokeLinecap="round" />
      </svg>
    ),
  },
```

Aggiornare il commento riga 40 a "LISTA / SETTIMANA / PIATTI / DISPENSA". In `impostazioni/page.tsx` rimuovere l'`<Etichetta ...>DISPENSA</Etichetta>` e l'intero `<Link href="/dispensa" ...>` che lo segue (il blocco parte a ~riga 341; la sezione REPERTORIO che segue resta).

- [ ] **Step 4: Run test + suite — atteso PASS.**
- [ ] **Step 5: Commit** — `fix(nav): Dispensa in tab bar, via la voce doppione da Impostazioni`

### Task 3: Testata — ingranaggio nominato e pillola senza freccetta

**Files:**
- Modify: `src/components/Testata.tsx:13-18` (commento), `:32-39` (link), `:51-53` (freccetta)
- Test: `src/components/__tests__/testata.test.tsx` (nuovo o esteso)

**Interfaces:**
- Produces: `Props` di `Testata` guadagna `indietro?: boolean` (default assente = comportamento di oggi). Consumer nuovo: `importa/page.tsx` (vedi Step 3). Gli altri chiamanti non cambiano.

- [ ] **Step 1: Test che fallisce**

```tsx
// src/components/__tests__/testata.test.tsx
import '@testing-library/jest-dom/vitest';
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { Testata } from '../Testata';

describe('Testata', () => {
  it('il link alle impostazioni ha nome accessibile e icona ingranaggio', () => {
    render(<Testata titolo="Lista" />);
    expect(screen.getByRole('link', { name: 'Impostazioni' })).toHaveAttribute('href', '/impostazioni');
  });

  it('la pillola settimana non ha la freccetta di un selettore che non esiste', () => {
    const { container } = render(<Testata titolo="Lista" settimana="31 AGO — 6 SET" />);
    expect(screen.getByText('31 AGO — 6 SET')).toBeInTheDocument();
    // l'unico svg ammesso è l'ingranaggio nel link: dentro la pillola niente svg
    const pillola = screen.getByText('31 AGO — 6 SET').parentElement!;
    expect(pillola.querySelector('svg')).toBeNull();
  });
});
```

- [ ] **Step 2: Run — atteso FAIL** (niente aria-label, svg nella pillola).

- [ ] **Step 3: Implementazione** — sul `<Link href="/impostazioni">` aggiungere `aria-label="Impostazioni"` e sostituire le tre linee burger con un ingranaggio line-art:

```tsx
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <circle cx="12" cy="12" r="3.1" stroke="var(--ink)" strokeWidth="1.9" />
            <path
              d="M12 2.8v2.4M12 18.8v2.4M21.2 12h-2.4M5.2 12H2.8M18.5 5.5l-1.7 1.7M7.2 16.8l-1.7 1.7M18.5 18.5l-1.7-1.7M7.2 7.2 5.5 5.5"
              stroke="var(--ink)" strokeWidth="1.9" strokeLinecap="round"
            />
          </svg>
```

Rimuovere l'`<svg>` della freccetta dalla pillola (righe 51-53) e aggiornare il commento del componente: la pillola è testo informativo; un selettore settimane, se mai, avrà una spec sua.

Due aggiunte alla stessa `Testata` (spec §A):
- il `Marchio` si avvolge in `<Link href="/lista" aria-label="Vai alla lista">` (il tocco sul marchio oggi non fa nulla; portarlo alla Lista è il "torna all'inizio" naturale);
- prop nuova opzionale `indietro?: boolean`: quando `true`, al posto del `Marchio` compare un bottone-link freccia (`<Link href="/impostazioni" aria-label="Indietro">`, chevron sinistro 20×20 stroke 2) e il link-ingranaggio a destra NON si renderizza. La usa solo `src/app/(app)/importa/page.tsx` (`<Testata titolo="Importa la dieta" indietro />`): Importa si raggiunge da Impostazioni e deve poterci tornare. Test aggiuntivo nello stesso file: con `indietro` c'è il link "Indietro" e non c'è "Impostazioni".

- [ ] **Step 4: Run test + suite — PASS.**
- [ ] **Step 5: Commit** — `fix(testata): ingranaggio con nome accessibile, pillola senza freccetta finta`

### Task 4: Settimana — rifiniture (errata audit)

**Files:**
- Modify: `src/components/RigaPasto.tsx:97-110` (aria-label corpo centrale)
- Modify: `src/components/StrisciaGiorni.tsx:5,34-51` (aria-label giorni)
- Modify: `src/app/(app)/settimana/page.tsx:528-532` (rimozione riga legenda, resta il contatore)
- Test: `src/components/__tests__/riga-pasto.test.tsx`, `src/components/__tests__/striscia-giorni.test.tsx` (nuovi o estesi se esistono già test per questi componenti — controllare `src/components/__tests__/`)

**Interfaces:** nessuna prop nuova.

- [ ] **Step 1: Test che fallisce**

```tsx
// striscia-giorni: aria-label coi nomi estesi
it('ogni giorno ha nome accessibile con giorno esteso, numero e stato', () => {
  render(
    <StrisciaGiorni giorni={['2026-08-24','2026-08-25','2026-08-26','2026-08-27','2026-08-28','2026-08-29','2026-08-30']}
      slotDefs={[]} slots={[]} oggi="2026-08-30" selezionato={6} onSeleziona={() => {}} />,
  );
  expect(screen.getByRole('button', { name: 'Domenica 30, selezionato' })).toBeInTheDocument();
  expect(screen.getByRole('button', { name: 'Lunedì 24' })).toBeInTheDocument();
});

// riga-pasto: il corpo centrale si chiama "Apri <piatto>"
it('il corpo centrale ha nome accessibile Apri quando c’è un piatto', () => {
  render(<RigaPasto nomePasto="Pranzo" stato="casa" nomePiatto="Riso e ceci" aree={[]}
    onToggleStato={() => {}} onApriPiatto={() => {}} hrefScegli="/x" />);
  expect(screen.getByRole('button', { name: 'Apri Riso e ceci' })).toBeInTheDocument();
});
```

(Import e wrapper `describe` come nei test di riferimento; per RigaPasto lo `stato` usa il tipo `StatoSlot` già esportato da `@/domain/types`.)

- [ ] **Step 2: Run — atteso FAIL.**

- [ ] **Step 3: Implementazione**

`StrisciaGiorni.tsx` — accanto a `LABEL_GIORNO` aggiungere:

```tsx
const NOME_GIORNO = ['Lunedì', 'Martedì', 'Mercoledì', 'Giovedì', 'Venerdì', 'Sabato', 'Domenica'];
```

e sul `<button>` del giorno:

```tsx
            aria-label={`${NOME_GIORNO[indice]} ${numero}${sel ? ', selezionato' : ''}`}
```

`RigaPasto.tsx` — sul bottone del corpo centrale (riga 97):

```tsx
        aria-label={nomePiatto ? `Apri ${nomePiatto}` : undefined}
```

(quando non c'è piatto il bottone resta `disabled` e anonimo com'è oggi: nessun nuovo comportamento).

`settimana/page.tsx` — rimuovere solo lo `<span>`/blocco della riga `CASA = A CASA · › APRE IL PIATTO` (riga ~531), lasciando il contatore `{nCasaSettimana} PASTI A CASA IN SETTIMANA`.

- [ ] **Step 4: Run test + suite — PASS** (aggiornare eventuali test esistenti che contavano la legenda).
- [ ] **Step 5: Commit** — `fix(settimana): nome accessibile su apri-piatto e giorni, via la legenda`

### Task 5: Piatto — vista di consultazione + MODIFICA

**Files:**
- Modify: `src/app/(app)/piatti/[id]/page.tsx` (stato `modalita`, componente interno `VistaPiatto`, testata con indietro; l'editor attuale resta identico e si monta solo in modifica)
- Test: `src/app/(app)/piatti/__tests__/vista.test.tsx` (nuovo)

**Interfaces:**
- Consumes: lo stato già presente nella pagina (`nome`, `slotDefs`, `slotDefId`, `settimanaCiclo`, `settimaneCiclo`, `giornoCiclo`, `ingredienti`, `componenti`, `catalogo`, `GIORNI_LABEL` riga 16, `GIORNI_LUNGHI` riga 17). `VistaPiatto` è una funzione interna al file: nessuna interfaccia esportata nuova (il file è già oltre le 400 righe; lo split Vista/Editor è annotato come follow-up, non si fa qui).
- Produces: la pagina apre in `modalita === 'vista'`; il bottone `MODIFICA` porta a `'modifica'`; `SALVA`/`ANNULLA` riportano a `'vista'` (ANNULLA ripristina lo stato dal `piattoOriginale` già in pagina, riusando la logica di caricamento esistente). La conferma di eliminazione esiste già (`confermaEliminazione`, riga 146 e 1044) e NON va toccata.

- [ ] **Step 1: Test che fallisce**

```tsx
// src/app/(app)/piatti/__tests__/vista.test.tsx — il contratto della vista:
// aprire un piatto NON mostra l'editor e NON emette scritture.
import '@testing-library/jest-dom/vitest';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';

const salvaPiatto = vi.fn();
const eliminaPiatto = vi.fn();
vi.mock('@/data/repertorio', async (orig) => ({
  ...(await orig<Record<string, unknown>>()),
  salvaPiatto,
  eliminaPiatto,
  // leggiRepertorio / leggiIngredienti: mock con un piatto minimo — copiare
  // la forma dai mock già usati nei test esistenti della pagina, se presenti
  // in src/app/(app)/piatti/__tests__/ o nei test della Revisione.
}));
// mock di next/navigation (useParams con l'id del piatto mockato, useRouter con back())

describe('dettaglio piatto: vista prima, modifica su richiesta', () => {
  it('apre in vista: niente input, niente SALVA, c’è MODIFICA', async () => {
    render(<Piatto />);
    expect(await screen.findByRole('button', { name: 'MODIFICA' })).toBeInTheDocument();
    expect(screen.queryByRole('button', { name: /SALVA PIATTO/ })).not.toBeInTheDocument();
    expect(screen.queryByRole('textbox')).not.toBeInTheDocument();
    expect(salvaPiatto).not.toHaveBeenCalled();
  });

  it('MODIFICA accende l’editor attuale', async () => {
    render(<Piatto />);
    (await screen.findByRole('button', { name: 'MODIFICA' })).click();
    expect(await screen.findByRole('button', { name: /SALVA PIATTO/ })).toBeInTheDocument();
  });
});
```

Nota per l'implementer: la pagina carica dati in un effect (`carica()`, riga 162) — i mock di `@/data/repertorio` devono restituire un piatto esistente perché la vista si monti; prendere la forma esatta dei dati dai tipi in `@/domain/types` (`Dish`, `Ingredient`, `MealSlotDef`) e dai mock dei test esistenti del progetto. Se un piatto NUOVO (rotta senza id / id assente) apre la pagina, deve aprire direttamente in `'modifica'`: un piatto che non esiste non ha niente da consultare.

- [ ] **Step 2: Run — atteso FAIL** (la pagina apre con l'editor).

- [ ] **Step 3: Implementazione**

1. Stato: `const [modalita, setModalita] = useState<'vista' | 'modifica'>('vista');` — inizializzato a `'modifica'` quando la pagina è per un piatto nuovo (stesso ramo che oggi distingue creazione da modifica nel `carica()`).
2. Componente interno, stesso file:

```tsx
/** Sola consultazione: il piano del nutrizionista si legge senza rischiare di cambiarlo. */
function VistaPiatto({ nome, pasto, settimana, giorno, righe, onModifica, onIndietro }: {
  nome: string;
  pasto: string;                      // nome dello slot, es. "Pranzo"
  settimana: string | null;           // es. "Settimana 2 del giro" o null se il giro è a 1
  giorno: string | null;              // es. "Giovedì" o null se LIBERO
  righe: { id: string; testo: string }[]; // "80 g · Riso" — composte dal chiamante
  onModifica: () => void;
  onIndietro: () => void;
}) {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 16, padding: '16px 18px' }}>
      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
        <button type="button" onClick={onIndietro} aria-label="Indietro"
          style={{ width: 44, height: 44, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
            <path d="M12.5 4 6.5 10l6 6" stroke="var(--ink)" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </button>
        <button type="button" onClick={onModifica}
          style={{ height: 40, padding: '0 18px', borderRadius: 999, background: 'var(--ink)',
            fontFamily: 'var(--font-mono)', fontSize: 10.5, fontWeight: 700, letterSpacing: '0.1em', color: '#FFFFFF' }}>
          MODIFICA
        </button>
      </div>
      <h1 style={{ margin: 0, fontSize: 34, fontWeight: 800, letterSpacing: '-0.04em', lineHeight: 1.05, color: 'var(--ink)' }}>
        {nome}
      </h1>
      <p style={{ margin: 0, fontFamily: 'var(--font-mono)', fontSize: 10.5, letterSpacing: '0.08em', color: 'var(--sec)' }}>
        {[pasto, settimana, giorno].filter(Boolean).join(' · ').toUpperCase()}
      </p>
      <ul style={{ margin: 0, padding: 0, listStyle: 'none', display: 'flex', flexDirection: 'column', gap: 8 }}>
        {righe.map((r) => (
          <li key={r.id} style={{ padding: '12px 14px', borderRadius: 14, background: '#FFFFFF',
            border: '1px solid rgba(20,22,58,0.07)', fontSize: 15.5, fontWeight: 600, color: 'var(--ink)' }}>
            {r.testo}
          </li>
        ))}
      </ul>
    </div>
  );
}
```

3. Nel render della pagina: `if (modalita === 'vista') return <VistaPiatto ... />` con: `pasto` dal nome dello slotDef selezionato; `settimana` = `settimaneCiclo > 1 && settimanaCiclo !== null ? \`Settimana ${settimanaCiclo} del giro\` : null`; `giorno` = `giornoCiclo !== null ? GIORNI_LUNGHI[giornoCiclo] : null`; `righe` composte da `ingredienti` (quantità+unità+nome dal `catalogo`) e dai `componenti` (una riga per opzione, prefissata dal nome del componente e da "oppure" tra le opzioni — stesso lessico della Revisione); `onModifica={() => setModalita('modifica')}`; `onIndietro={() => router.back()}` (`useRouter` già importato o da aggiungere da `next/navigation`).
4. `SALVA` con successo e `ANNULLA` fanno `setModalita('vista')` in aggiunta a quello che già fanno; ANNULLA su piatto nuovo continua a uscire dalla pagina come oggi.
5. I chip giorno dell'editor (riga 583-584): sostituire `GIORNI_INIZIALE` con `GIORNI_LABEL` (già definito a riga 16) e cancellare `GIORNI_INIZIALE` (riga 23). Le descrizioni `GIORNI_LUNGHI` restano.

- [ ] **Step 4: Run test + suite — PASS** (aggiornare i test esistenti della pagina che si aspettano l'editor all'apertura, se ci sono).
- [ ] **Step 5: Commit** — `feat(piatto): vista di consultazione, editor solo su MODIFICA, chip giorno a tre lettere`

### Task 6: Piatti — ricerca e titoli a due righe

**Files:**
- Modify: `src/app/(app)/piatti/page.tsx` (stato `ricerca`, campo sopra i filtri, filtro per nome; titolo card a 2 righe)
- Test: `src/app/(app)/piatti/__tests__/ricerca.test.tsx` (nuovo)

**Interfaces:** nessuna esportazione nuova; helper locale `normalizza` copiato dal pattern di `piatti/[id]/page.tsx:98` (minuscole + rimozione accenti via `normalize('NFD')`), NON importato da `src/domain/import/mapping.ts` (il dominio import resta intoccato, Global Constraints).

- [ ] **Step 1: Test che fallisce**

```tsx
// src/app/(app)/piatti/__tests__/ricerca.test.tsx
import '@testing-library/jest-dom/vitest';
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
// mock di @/data/repertorio con due piatti: "Riso con frittata" e "Pesce al forno"
// (forma dei dati dai tipi Dish/MealSlotDef in @/domain/types, come nei mock dei
// test esistenti del progetto); mock di next/navigation come nel task 2.

describe('ricerca piatti', () => {
  it('filtra per nome, accenti e maiuscole ignorati', async () => {
    render(<Piatti />);
    await screen.findByText('Riso con frittata');
    fireEvent.change(screen.getByRole('searchbox', { name: 'Cerca un piatto' }), {
      target: { value: 'PÉSCE' },
    });
    await waitFor(() => expect(screen.queryByText('Riso con frittata')).not.toBeInTheDocument());
    expect(screen.getByText('Pesce al forno')).toBeInTheDocument();
  });
});
```
- [ ] **Step 2: Run — FAIL.**
- [ ] **Step 3: Implementazione** — `const [ricerca, setRicerca] = useState('');` + `<input type="search" placeholder="Cerca un piatto" aria-label="Cerca un piatto" ...>` stilizzato come i campi esistenti della pagina, sopra la riga dei filtri; filtro `piatti.filter(p => normalizza(p.nome).includes(normalizza(ricerca)))` composto col filtro pasto esistente; sul titolo card sostituire `whiteSpace/textOverflow` a riga singola con clamp a 2 righe (`display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden'`).
- [ ] **Step 4: Run + suite — PASS.**
- [ ] **Step 5: Commit** — `feat(piatti): ricerca per nome e titoli a due righe`

### Task 7: Dispensa — inventario prima, correzione AI in fondo e compressa

**Files:**
- Modify: `src/app/(app)/dispensa/page.tsx:237-241` (spostamento blocco), `:240` (paragrafo)
- Test: `src/app/(app)/dispensa/__tests__/ordine.test.tsx` (nuovo) o estensione dei test esistenti della pagina

**Interfaces:** `NotaDispensa` NON cambia firma (`contesto`, `onDatiCambiati`): si monta solo quando la card è espansa.

- [ ] **Step 1: Test che fallisce** — render della pagina (mock dati con 1 voce in casa): l'ordine nel DOM è IN CASA → MAI COMPRATI → card "Il conto non torna?"; la textarea della nota NON è nel documento finché non si tocca la card; dopo il tap c'è (`findByRole('textbox')`).
- [ ] **Step 2: Run — FAIL** (oggi NotaDispensa è in cima e sempre espansa).
- [ ] **Step 3: Implementazione**

1. Stato `const [notaAperta, setNotaAperta] = useState(false);`
2. Spostare il blocco `<NotaDispensa .../>` DOPO la sezione MAI COMPRATI (dopo il blocco che parte a ~riga 264; la sezione PRONTI, ~riga 376, resta dov'è tra IN CASA e MAI COMPRATI se già così — l'ordine finale è IN CASA → PRONTI → MAI COMPRATI → nota), avvolto così:

```tsx
        {notaAperta ? (
          <NotaDispensa contesto={contestoNota} onDatiCambiati={ricarica} />
        ) : (
          <button type="button" onClick={() => setNotaAperta(true)}
            style={{ width: '100%', padding: '15px 16px', borderRadius: 18, background: '#FFFFFF',
              border: '1px solid rgba(20,22,58,0.07)', textAlign: 'left',
              fontSize: 14.5, fontWeight: 600, color: 'var(--ink)' }}>
            Il conto non torna? Correggi con una nota
          </button>
        )}
```

3. Il paragrafo lungo (riga 240) si riduce a una riga sotto l'intestazione IN CASA: `Calcolato da spesa e piano: correggi solo se non torna con la realtà.` (stesso stile del paragrafo attuale).
4. Dentro `NotaDispensa`, lo stato disabilitato del bottone Correggi prende `opacity: 0.35` (allineato al pattern di RIPARTI in Impostazioni) così si distingue dall'attivo — trovare il bottone in `src/components/NotaDispensa.tsx` (grep "Correggi") e toccare SOLO lo stile del ramo disabled.

- [ ] **Step 4: Run + suite — PASS** (aggiornare i test E2E/component della dispensa che assumono la nota in cima, se esistono in `src/app/(app)/dispensa/__tests__/`).
- [ ] **Step 5: Commit** — `fix(dispensa): inventario prima, correzione AI in fondo e a scomparsa`

**Gate di fine Lotto 1:** suite completa verde + giro browser su mobile (375×812): `/` → lista, 4 tab attive, vista piatto, dispensa riordinata. Solo dopo si parte col Lotto 2.

## Lotto 2 — coerenza e animazioni

### Task 8: Lotto copy e accessibilità (batch, un solo dispatch)

**Files:**
- Modify: `src/app/(app)/lista/page.tsx:421` e `:477-490` (SelettoreTab)
- Modify: `src/components/FoglioAzioniPasto.tsx:124,128` + il chiamante in `src/app/(app)/settimana/page.tsx`
- Modify: `src/app/(app)/impostazioni/page.tsx:320-338` (copy giro + conferma RIPARTI) e testata (doppio titolo)
- Modify: `src/app/(app)/importa/Camera.tsx:204-227` (input file vestito)
- Test: estensioni dei test esistenti di ciascun file (camera.test.tsx c'è già; per gli altri seguire il pattern del progetto)

**Interfaces:** `FoglioAzioniPasto` prende una prop nuova `haPiatto: boolean`; il chiamante in settimana/page.tsx la passa come `slot.dishId !== null` per lo slot del foglio aperto. Nessun'altra firma cambia.

Modifiche, una per riga (test prima per le prime tre, poi implementazione in blocco):

1. `lista/page.tsx:421`: `{sezione.voci.length} {sezione.voci.length === 1 ? 'VOCE' : 'VOCI'}`.
2. `SelettoreTab`: `aria-label={\`Base, ${daPrendereBase} da prendere\`}` sul primo bottone, `aria-label={\`Top-up, ${daPrendereTopup} da prendere\`}` sul secondo.
3. `FoglioAzioniPasto.tsx:124`: `Ho mangiato altro` → `Ho mangiato fuori piano`; riga 128: `{aCasa && <Voce ...>` → `{aCasa && haPiatto && <Voce ...>` (prop nuova; "Cucinato ma non mangiato" senza piatto non ha senso).
4. `impostazioni/page.tsx:320`: il copy del giro sceglie il verbo dalla data: `cicloOrigine > oggi ISO` → "Il giro comincia lunedì …", altrimenti "Il giro è cominciato lunedì …" (le due stringhe complete, stesso resto della frase).
5. RIPARTI (riga 323-338): conferma in due tocchi col pattern già usato dal cestino del piatto (`confermaEliminazione`, `piatti/[id]/page.tsx:1044`): primo tap → il testo del bottone diventa `SICURO? RIPARTI DA LUNEDÌ` e un secondo tap entro il componente esegue `persistiCiclo`; un tap fuori o un cambio di stato annulla.
6. Doppio titolo Impostazioni: rimuovere l'eyebrow "IMPOSTAZIONI" piccolo sopra l'H1 (resta solo il titolo grande) — è nella testata della pagina, grep `IMPOSTAZIONI`.
7. `Camera.tsx` fallback: input nascosto + label-bottone nello stile dell'app:

```tsx
          La fotocamera non è disponibile: scegli le foto dei fogli
          <span style={{ alignSelf: 'flex-start', height: 40, padding: '0 18px', borderRadius: 999,
            display: 'inline-flex', alignItems: 'center', background: 'var(--ink)',
            fontFamily: 'var(--font-mono)', fontSize: 10.5, fontWeight: 700, letterSpacing: '0.1em', color: '#FFFFFF' }}>
            SCEGLI LE FOTO
          </span>
          <input type="file" accept="image/*" multiple capture="environment"
            aria-label="scegli le foto dei fogli" onChange={scegliFile}
            style={{ position: 'absolute', width: 1, height: 1, opacity: 0, overflow: 'hidden', clipPath: 'inset(50%)' }} />
```

(il `<label>` contenitore resta: il tap sullo span-bottone apre il picker; il test camera esistente che usa `findByLabelText(/scegli le foto/i)` continua a passare perché l'aria-label resta).

- [ ] **Step 1-2:** test che falliscono per: VOCE singolare; `haPiatto=false` nasconde "Cucinato ma non mangiato"; RIPARTI non chiama `persistiCiclo` al primo tap. Run — FAIL.
- [ ] **Step 3:** implementare le 7 modifiche.
- [ ] **Step 4:** run test + suite — PASS (camera.test e i test del foglio azioni esistenti aggiornati dove cambiano le stringhe).
- [ ] **Step 5: Commit** — `fix(ui): lotto coerenza — copy, plurali, conferme e nomi accessibili`

### Task 9: Animazioni causa-effetto

**Files:**
- Modify: `src/app/globals.css` (blocco in coda)
- Modify: `src/components/Tessera.tsx`, `src/components/RigaPasto.tsx`, `src/components/FoglioAzioniPasto.tsx`, `src/app/(app)/dispensa/page.tsx` (classi), `src/app/(app)/settimana/page.tsx` (chiave sul contenuto giorno)
- Test: nessun test automatico (transizioni CSS); verifica nel browser al Task 10. La suite resta verde.

**Interfaces:** solo classi CSS nuove, prefisso `anim-`.

- [ ] **Step 1:** in coda a `globals.css`:

```css
/* Animazioni causa-effetto (spec review UI §F). Tutte e sole qui, e tutte
   dentro il media: chi chiede meno moto non ne riceve nessuno. */
@media (prefers-reduced-motion: no-preference) {
  .anim-stato { transition: background 180ms ease, color 180ms ease, opacity 180ms ease; }
  .anim-foglio { animation: anim-sali 200ms ease; }
  .anim-apparsa { animation: anim-fade 180ms ease; }
  .anim-giorno { animation: anim-fade 160ms ease; }
  @keyframes anim-sali { from { transform: translateY(14px); opacity: 0.6; } to { transform: none; opacity: 1; } }
  @keyframes anim-fade { from { opacity: 0; } to { opacity: 1; } }
}
```

- [ ] **Step 2:** applicare: `className="anim-stato"` al contenitore radice di `Tessera` e di `RigaPasto` (spunta e toggle casetta transiscono); `className="anim-foglio"` al pannello del foglio azioni (FoglioAzioniPasto) e alla `NotaDispensa` quando si espande (sul wrapper del Task 7); `className="anim-apparsa"` alla riga sottotitolo "+N porzioni" in RigaPasto (riga 139-154) e `className="anim-giorno"` + `key={giornoSelezionato}` sul contenitore dei pasti del giorno in settimana/page.tsx (la key forza il remount → fade al cambio giorno; il contenuto è leggero, niente jank).
- [ ] **Step 3:** `npm test` — verde (nessun comportamento cambia).
- [ ] **Step 4: Commit** — `feat(ui): animazioni causa-effetto sotto prefers-reduced-motion`

### Task 10: Censimento accessibilità e verifica finale

**Files:** eventuali fix puntuali emersi; nessun file predefinito.

- [ ] **Step 1:** dev server + browser mobile 375×812. Giro completo: `/` (→ lista), lista (BASE/TOP-UP, spunta con transizione), settimana (striscia nominata, foglio azioni animato), piatto (vista → MODIFICA → ANNULLA), piatti (ricerca), dispensa (ordine, card AI a scomparsa), impostazioni (RIPARTI in due tocchi, un titolo solo), importa (bottone SCEGLI LE FOTO).
- [ ] **Step 2:** censimento con `read_page` (o ispezione accessibility tree) su lista, settimana, piatti, dispensa, importa: ogni bottone/link interattivo ha un nome. Fix puntuali per gli anonimi residui, col pattern aria-label del progetto.
- [ ] **Step 3:** `npm test` + `npx tsc --noEmit` + `npx eslint src` — tutto verde.
- [ ] **Step 4: Commit** — `fix(a11y): nomi accessibili residui dal censimento finale`

## Follow-up annotati (fuori piano)

- Split `piatti/[id]/page.tsx` in Vista/Editor quando si rimette mano alla pagina.
- Dark mode (spec dedicata). Selettore/storico settimane (spec dedicata).
