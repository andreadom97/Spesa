# Spesa — Fase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trasformare un repertorio di piatti inserito a mano e un check-in settimanale in una lista della spesa ordinata per area, con quantità in confezioni derivate dal residuo di dispensa.

**Architecture:** Tre strati con confini netti. `src/domain/` è TypeScript puro senza rete, DB o React: ci vivono `list-builder` e `pantry`, il cuore testabile del prodotto, in TDD. `src/data/` è l'unico strato che parla con Supabase e converte righe in tipi di dominio. `src/app/` è Next.js App Router e non contiene aritmetica. Zero AI in Fase 1.

**Tech Stack:** Next.js (App Router) + TypeScript, Tailwind, Supabase (Postgres + Auth + RLS), Vitest, PWA installabile su Android con coda offline sulla lista. Node v24.14.0 e npm 11.9.0 verificati sulla macchina [misurato ora, `node -v` / `npm -v`].

**Spec:** [`docs/superpowers/specs/2026-08-26-spesa-design.md`](../specs/2026-08-26-spesa-design.md)
**Sistema visivo:** [`docs/superpowers/specs/DESIGN-SYSTEM.md`](../specs/DESIGN-SYSTEM.md)
**Schermate:** `design/*.dc.html` — vedi "Regola sugli artboard" sotto.

---

## Global Constraints

Valgono per ogni task. Valori copiati alla lettera dalla spec e dal sistema visivo.

- **Multi-utente dal giorno uno.** Ogni tabella ha `user_id` e row-level security attiva. Nessuna eccezione, nemmeno sulle tabelle figlie.
- **Nessun inventario.** Il residuo è sempre derivato (`residuo precedente + comprato − consumato dal piano`), mai inserito dall'utente. Non esiste nessuna schermata che accetti un residuo come input.
- **Sei aree fisse**, in quest'ordine di default: ortofrutta `#A8D96A`, macelleria e pescheria `#F29B9B`, latticini/uova/salumi `#9CC7F2`, pasta/riso/cereali `#F5CE5B`, dispensa e conserve `#F2A465`, surgelati `#B9AEF5`. È personalizzabile **solo l'ordine di apparizione**. Aggiungere o togliere un'area rompe il marchio.
- **Da 3 a 5 pasti**, definiti dall'utente e riordinabili (`meal_slot_def`). Nessuna schermata può assumere quattro colonne o l'enum colazione/pranzo/cena/spuntino.
- **Regola del marchio:** ogni casella è sempre nel colore della sua area — piena quando in quell'area non manca niente (completata **o** assente dalla spesa), contornata nello stesso colore quando manca qualcosa. **Mai grigia.**
- **Zero AI.** Nessuna chiamata LLM in Fase 1. La tabella `correction` non viene creata.
- **Zero integrazioni.** Niente Google Calendar, niente posizione, niente cataloghi supermercato, niente prodotti non alimentari.
- **Niente macro né calorie.** Le colonne `kcal_100`, `prot_100`, `carb_100`, `gras_100` esistono su `ingredient` ma non vengono mai popolate né lette.
- **Nessun gradiente in nessun punto.** Ombre solo su superfici scure e tessere accese. Bersagli tattili mai sotto 44px.
- **Tipografia:** Plus Jakarta Sans (400/500/600/700/800) per testo e titoli, JetBrains Mono (400/500/700) per etichette di servizio, quantità e contatori.
- **Lingua:** italiano ovunque — nomi di dominio, copy, commenti. I file `.dc.html` sono già in italiano; il codice li segue.
- **Il selettore settimana non naviga.** La freccia in testata è presente ma inerte: la v1 conosce solo la settimana corrente.

### Regola sugli artboard

Per ogni task di UI, **l'artboard `.dc.html` corrispondente è la specifica visiva vincolante**: colori, spaziature, raggi, dimensioni dei font e copy si leggono da lì, non da questo piano. Il piano dà struttura, dati, stati e criteri di accettazione. Se un valore in questo piano diverge dall'artboard, **vince l'artboard**. Gli artboard usano un formato proprio (`x-dc`, `{{holes}}`, `sc-for`): si leggono come riferimento, non si copiano meccanicamente — la logica `renderVals()` va riscritta in React.

---

## Divergenze dalla spec — leggere prima di iniziare

Quattro punti dove la spec confligge con sé stessa o tace. Ognuno è risolto qui con la sua ragione. Se una risoluzione è sbagliata, va corretta **prima** del task che la usa.

**1. Griglia 7×4 contro pasti variabili — risolto a favore dei pasti variabili.**
La sezione *Fasi* dice «check-in settimanale manuale su griglia 7×4». La *Revisione del secondo giro* elimina l'enum a quattro slot e lo sostituisce con `meal_slot_def`, da 3 a 5 righe per utente. La spec dichiara che le decisioni chiuse hanno precedenza sulle assunzioni precedenti, quindi la griglia è **7×N con N fra 3 e 5**. Conseguenza sul lavoro: `meal_slot_def` entra in Fase 1 con la sua UI di riordino nelle Impostazioni (Task 6, 16), non è un enum.

**2. Controllo staple: `giorni_stimati × 0.8` contro 90 giorni fissi — risolto a favore dei 90 fissi.**
La regola 7 di `list-builder` prevede una soglia all'80% di una stima per categoria (olio 60, pasta 21, caffè 30 → scatterebbe a 48, 17, 24 giorni). La decisione chiusa dice intervallo fisso di 90 giorni senza apprendimento, e i due artboard confermano: `Lista.dc.html` scrive `CONTROLLO OGNI 90 GIORNI · SCADUTO`, `Ingrediente.dc.html` scrive «Ogni 90 giorni dall'ultimo acquisto la lista ti chiede se ne hai ancora». Vale **90 giorni fissi**. `giorni_stimati` resta nel modello come costante, non usata dall'aritmetica di Fase 1.

**3. Cosa fa la risposta "sì" al controllo — la spec non lo dice più, decisione presa qui.**
La regola 7 diceva che "sì" allunga `giorni_stimati`; con l'intervallo fisso quella leva non esiste più, e senza sostituto il controllo ricomparirebbe ogni settimana per sempre. **Decisione:** "sì" scrive `ultimo_check = oggi`, e il controllo scatta quando sono passati 90 giorni dal più recente fra `ultimo_acquisto` e `ultimo_check`. Usa solo campi già previsti dalla spec e non introduce costanti nuove. "no" resta come da spec: genera una riga d'acquisto di una confezione.

**4. Manca una tabella per lo storico acquisti — aggiunta qui.**
La spec impone che «la v1 registri comunque ogni acquisto (cosa e quando)» per non far ripartire la Fase 4 da zero, ma l'elenco delle tabelle ha solo `pantry_state.ultimo_acquisto`, che conserva un valore singolo e non uno storico. **Decisione:** si aggiunge la tabella `purchase` (`ingredient_id`, `data`, `confezioni`, `quantita`), scritta alla chiusura della spesa. `pantry_state.ultimo_acquisto` resta come cache denormalizzata per il controllo a 90 giorni.

**Due regole di presentazione che la spec non fissa** (i dati mock degli artboard sono incoerenti su entrambe, perché scritti a mano per varietà visiva):
- **Sottotitolo `serve X · in casa Y`:** compare sulle voci di classe `porzionabile` e solo su quelle. Sono i casi in cui l'aritmetica non è ovvia; `intero` si spiega da sé (sei uova sono sei uova).
- **Tessera protagonista:** è la prima voce di ogni area. Le voci dentro un'area sono ordinate per numero di confezioni decrescente, poi per nome — quindi la protagonista è l'acquisto più grosso di quell'area, in modo deterministico.

---

## File Structure

```
src/
  domain/                    TypeScript puro. Nessun import da next, react, supabase.
    types.ts                 Tutti i tipi del dominio
    aree.ts                  Le sei aree fisse, colori, ordine di default
    unita.ts                 Conversione fra unità, errore su incompatibili
    date.ts                  giorniTra, lunedìDi, giorniDellaSettimana
    pantry.ts                nuovoResiduo, serveControllo
    list-builder.ts          costruisciLista — il cuore
    planner.ts               assegnaPiatti
    week-shape.ts            generaSettimana, applicaStato
  data/                      Unico strato che parla con Supabase
    supabase.ts              Client browser + server
    repertorio.ts            Piatti e ingredienti
    settimana.ts             Settimane e slot
    lista.ts                 Liste della spesa e voci
    dispensa.ts              pantry_state e purchase
    impostazioni.ts          settings e meal_slot_def
  app/                       Next.js App Router. Nessuna aritmetica qui.
    layout.tsx  globals.css
    (app)/layout.tsx         Shell con TabBar
    (app)/lista/page.tsx
    (app)/settimana/page.tsx
    (app)/settimana/[data]/[slotDefId]/scegli/page.tsx
    (app)/piatti/page.tsx
    (app)/piatti/[id]/page.tsx
    (app)/piatti/[id]/ingredienti/[ingId]/page.tsx
    (app)/impostazioni/page.tsx
    (app)/impostazioni/reparti/page.tsx
  components/
    Marchio.tsx  Testata.tsx  TabBar.tsx
    Tessera.tsx  RigaControllo.tsx  Vuoto.tsx  Segmento.tsx
  offline/
    coda.ts                  Coda locale delle spunte, id idempotenti
supabase/migrations/         SQL versionato
public/                      manifest.json, sw.js, icone
```

Ogni file di `src/domain/` è sotto le 200 righe e ha un test accanto in `src/domain/__tests__/`. Il confine è verificabile: nessun file in `domain/` importa da `data/` o `app/`.

---

## Task 1: Scaffolding, token visivi e Vitest

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `vitest.config.ts`
- Create: `src/app/globals.css`, `src/app/layout.tsx`
- Create: `src/domain/__tests__/smoke.test.ts`
- Create: `.env.local.example`

**Interfaces:**
- Consumes: niente (primo task)
- Produces: comando `npm test` funzionante; le CSS custom properties dei token, usate da ogni task di UI

- [ ] **Step 1: Creare il progetto Next.js**

Dalla radice del repo (che contiene già `.git`, `README.md`, `design/`):

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
```

Se il comando protesta che la directory non è vuota, accettare di procedere: non tocca `design/`, `docs/` né `README.md`. Verificare subito dopo che quei tre siano ancora al loro posto.

- [ ] **Step 2: Installare Vitest e configurarlo**

```bash
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
```

`vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['src/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
});
```

In `package.json`, aggiungere agli script:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 3: Scrivere il test di smoke**

`src/domain/__tests__/smoke.test.ts`:

```ts
import { describe, it, expect } from 'vitest';

describe('toolchain', () => {
  it('esegue i test', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 4: Eseguire i test**

Run: `npm test`
Expected: PASS, 1 test.

- [ ] **Step 5: Definire i token visivi come CSS custom properties**

I token vanno in `globals.css` come variabili CSS, non nella configurazione di Tailwind: funzionano identici su Tailwind 3 e 4, e rispecchiano il modo in cui gli artboard scrivono i colori. Nel JSX si usano con valori arbitrari, `bg-[var(--ink)]`.

In `src/app/globals.css`, sotto le direttive di Tailwind già generate:

```css
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap');

:root {
  --fondo: #F1F0EE;
  --superficie: #FFFFFF;
  --ink: #14163A;
  --ink-2: #3D4166;
  --sec: #8A8A96;
  --ter: #A6A6B2;
  --off: #9A9AA6;
  --bordo: rgba(20, 22, 58, 0.07);
  --spento: rgba(20, 22, 58, 0.035);

  --area-ortofrutta: #A8D96A;
  --area-macelleria: #F29B9B;
  --area-latticini: #9CC7F2;
  --area-cereali: #F5CE5B;
  --area-dispensa: #F2A465;
  --area-surgelati: #B9AEF5;

  --font-testo: 'Plus Jakarta Sans', 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'JetBrains Mono', ui-monospace, monospace;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  background: var(--fondo);
  color: var(--ink);
  font-family: var(--font-testo);
  -webkit-font-smoothing: antialiased;
}

button { font-family: inherit; border: 0; background: none; padding: 0; cursor: pointer; text-align: left; }
```

- [ ] **Step 6: Verificare che l'app parta**

Run: `npm run dev`, aprire `http://localhost:3000`
Expected: la pagina di default carica con fondo `#F1F0EE`. Fermare il server.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "chore: scaffolding Next.js, Vitest e token visivi"
```

---

## Task 2: Tipi del dominio, aree e conversione unità

**Files:**
- Create: `src/domain/types.ts`
- Create: `src/domain/aree.ts`
- Create: `src/domain/unita.ts`
- Create: `src/domain/date.ts`
- Test: `src/domain/__tests__/unita.test.ts`, `src/domain/__tests__/date.test.ts`

**Interfaces:**
- Consumes: niente
- Produces: **tutti i tipi usati da ogni task successivo.** `AreaId`, `UnitaBase`, `UnitaMisura`, `ClasseResiduo`, `StatoSlot`, `FonteStato`, `Ingredient`, `DishIngredient`, `Dish`, `MealSlotDef`, `MealSlot`, `PantryState`, `Impostazioni`. Più `AREE`, `ORDINE_AREE_DEFAULT`, `coloreArea()`, `nomeArea()`, `convertiInUnitaBase()`, `UnitaIncompatibileError`, `giorniTra()`, `lunediDi()`, `giorniDellaSettimana()`.

- [ ] **Step 1: Scrivere i tipi**

`src/domain/types.ts`:

```ts
export type UnitaBase = 'g' | 'ml' | 'pz';
export type UnitaMisura = 'g' | 'kg' | 'ml' | 'l' | 'pz';
export type ClasseResiduo = 'porzionabile' | 'intero' | 'stima';
export type AreaId =
  | 'ortofrutta' | 'macelleria' | 'latticini'
  | 'cereali' | 'dispensa' | 'surgelati';

export type StatoSlot = 'casa' | 'fuori' | 'saltato';
export type FonteStato = 'default' | 'calendario' | 'checkin' | 'correzione';
export type FonteDish = 'nutrizionista' | 'proprio';

export interface Ingredient {
  id: string;
  nome: string;
  unitaBase: UnitaBase;
  area: AreaId;
  classeResiduo: ClasseResiduo;
  deperibile: boolean;
  /** Quantità di una confezione, espressa in unitaBase. */
  formatoConfezione: number;
}

export interface DishIngredient {
  ingredientId: string;
  /** La porzione del piano per un commensale. */
  quantita: number;
  unita: UnitaMisura;
}

export interface Dish {
  id: string;
  nome: string;
  slotDefId: string;
  fonte: FonteDish;
  attivo: boolean;
  ingredienti: DishIngredient[];
}

export interface MealSlotDef {
  id: string;
  nome: string;
  posizione: number;
  /** Sette booleani, indice 0 = lunedì. true = abitualmente fuori casa. */
  assenzeAbituali: boolean[];
}

export interface MealSlot {
  id: string;
  /** ISO yyyy-mm-dd */
  data: string;
  slotDefId: string;
  stato: StatoSlot;
  dishId: string | null;
  fonteStato: FonteStato;
}

export interface PantryState {
  ingredientId: string;
  /** In unitaBase dell'ingrediente. Mai negativo. */
  residuo: number;
  /** ISO yyyy-mm-dd, null se mai comprato. */
  ultimoAcquisto: string | null;
  /** Costante 90 in Fase 1. Presente per la Fase 4. */
  giorniStimati: number;
  /** ISO yyyy-mm-dd dell'ultima risposta "sì" a un controllo. */
  ultimoCheck: string | null;
}

export interface Impostazioni {
  moltiplicatorePorzioni: number;
  ordineAree: AreaId[];
}
```

- [ ] **Step 2: Scrivere le aree**

`src/domain/aree.ts`:

```ts
import type { AreaId } from './types';

export interface Area {
  id: AreaId;
  nome: string;
  colore: string;
}

/** Fisse. Aggiungerne o toglierne rompe il marchio a griglia 3×2. */
export const AREE: readonly Area[] = [
  { id: 'ortofrutta', nome: 'ORTOFRUTTA', colore: '#A8D96A' },
  { id: 'macelleria', nome: 'MACELLERIA E PESCHERIA', colore: '#F29B9B' },
  { id: 'latticini', nome: 'LATTICINI, UOVA E SALUMI', colore: '#9CC7F2' },
  { id: 'cereali', nome: 'PASTA, RISO E CEREALI', colore: '#F5CE5B' },
  { id: 'dispensa', nome: 'DISPENSA E CONSERVE', colore: '#F2A465' },
  { id: 'surgelati', nome: 'SURGELATI', colore: '#B9AEF5' },
] as const;

export const ORDINE_AREE_DEFAULT: AreaId[] = AREE.map((a) => a.id);

const PER_ID = new Map(AREE.map((a) => [a.id, a]));

export function coloreArea(id: AreaId): string {
  const a = PER_ID.get(id);
  if (!a) throw new Error(`Area sconosciuta: ${id}`);
  return a.colore;
}

export function nomeArea(id: AreaId): string {
  const a = PER_ID.get(id);
  if (!a) throw new Error(`Area sconosciuta: ${id}`);
  return a.nome;
}

/**
 * L'ordine delle caselle del marchio: la griglia 2×3 verticale ruotata di 90°
 * in senso orario. Riga 1 arancio/azzurro/verde, riga 2 lilla/giallo/corallo.
 * Fisso, indipendente dall'ordine scelto dall'utente per la lista.
 */
export const ORDINE_MARCHIO: AreaId[] = [
  'dispensa', 'latticini', 'ortofrutta',
  'surgelati', 'cereali', 'macelleria',
];
```

- [ ] **Step 3: Scrivere il test delle unità (fallirà)**

`src/domain/__tests__/unita.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { convertiInUnitaBase, UnitaIncompatibileError } from '../unita';

describe('convertiInUnitaBase', () => {
  it('lascia invariata una quantità già nella base', () => {
    expect(convertiInUnitaBase(150, 'g', 'g')).toBe(150);
  });

  it('converte i chili in grammi', () => {
    expect(convertiInUnitaBase(1.2, 'kg', 'g')).toBe(1200);
  });

  it('converte i litri in millilitri', () => {
    expect(convertiInUnitaBase(0.75, 'l', 'ml')).toBe(750);
  });

  it('lascia invariati i pezzi', () => {
    expect(convertiInUnitaBase(6, 'pz', 'pz')).toBe(6);
  });

  it('rifiuta grammi su un ingrediente contato a pezzi', () => {
    expect(() => convertiInUnitaBase(100, 'g', 'pz')).toThrow(UnitaIncompatibileError);
  });

  it('rifiuta millilitri su un ingrediente in grammi: niente inferenza di densità', () => {
    expect(() => convertiInUnitaBase(100, 'ml', 'g')).toThrow(UnitaIncompatibileError);
  });
});
```

- [ ] **Step 4: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/domain/__tests__/unita.test.ts`
Expected: FAIL — `Failed to resolve import "../unita"`.

- [ ] **Step 5: Implementare la conversione**

`src/domain/unita.ts`:

```ts
import type { UnitaBase, UnitaMisura } from './types';

export class UnitaIncompatibileError extends Error {
  constructor(da: UnitaMisura, a: UnitaBase) {
    super(`Non converto ${da} in ${a}: servirebbe una densità e la spec vieta l'inferenza.`);
    this.name = 'UnitaIncompatibileError';
  }
}

const FATTORI: Record<UnitaMisura, { base: UnitaBase; fattore: number }> = {
  g: { base: 'g', fattore: 1 },
  kg: { base: 'g', fattore: 1000 },
  ml: { base: 'ml', fattore: 1 },
  l: { base: 'ml', fattore: 1000 },
  pz: { base: 'pz', fattore: 1 },
};

export function convertiInUnitaBase(quantita: number, da: UnitaMisura, base: UnitaBase): number {
  const f = FATTORI[da];
  if (!f || f.base !== base) throw new UnitaIncompatibileError(da, base);
  return quantita * f.fattore;
}
```

- [ ] **Step 6: Eseguire il test**

Run: `npx vitest run src/domain/__tests__/unita.test.ts`
Expected: PASS, 6 test.

- [ ] **Step 7: Scrivere il test delle date (fallirà)**

`src/domain/__tests__/date.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { giorniTra, lunediDi, giorniDellaSettimana } from '../date';

describe('giorniTra', () => {
  it('conta i giorni fra due date ISO', () => {
    expect(giorniTra('2026-08-26', '2026-08-31')).toBe(5);
  });

  it('attraversa il cambio di mese', () => {
    expect(giorniTra('2026-08-26', '2026-11-24')).toBe(90);
  });

  it('attraversa il cambio dell\'ora legale senza sbagliare di un giorno', () => {
    expect(giorniTra('2026-10-20', '2026-11-03')).toBe(14);
  });

  it('è zero sullo stesso giorno', () => {
    expect(giorniTra('2026-08-26', '2026-08-26')).toBe(0);
  });
});

describe('lunediDi', () => {
  it('restituisce sé stesso su un lunedì', () => {
    expect(lunediDi('2026-08-31')).toBe('2026-08-31');
  });

  it('torna indietro al lunedì da una domenica', () => {
    expect(lunediDi('2026-09-06')).toBe('2026-08-31');
  });

  it('torna indietro al lunedì da un mercoledì', () => {
    expect(lunediDi('2026-09-02')).toBe('2026-08-31');
  });
});

describe('giorniDellaSettimana', () => {
  it('produce sette date consecutive a partire dal lunedì', () => {
    expect(giorniDellaSettimana('2026-08-31')).toEqual([
      '2026-08-31', '2026-09-01', '2026-09-02', '2026-09-03',
      '2026-09-04', '2026-09-05', '2026-09-06',
    ]);
  });
});
```

- [ ] **Step 8: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/domain/__tests__/date.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 9: Implementare le date**

Tutta l'aritmetica passa per UTC: sommare 24 ore a un `Date` locale sbaglia di un'ora nelle notti di cambio ora legale, e quell'ora diventa un giorno sbagliato dopo l'arrotondamento.

`src/domain/date.ts`:

```ts
function aUtc(iso: string): number {
  const ms = Date.parse(`${iso}T00:00:00Z`);
  if (Number.isNaN(ms)) throw new Error(`Data ISO non valida: ${iso}`);
  return ms;
}

function daUtc(ms: number): string {
  return new Date(ms).toISOString().slice(0, 10);
}

const GIORNO_MS = 86_400_000;

/** Giorni pieni da `da` ad `a`. Negativo se `a` precede `da`. */
export function giorniTra(da: string, a: string): number {
  return Math.round((aUtc(a) - aUtc(da)) / GIORNO_MS);
}

export function sommaGiorni(iso: string, n: number): string {
  return daUtc(aUtc(iso) + n * GIORNO_MS);
}

/** Il lunedì della settimana che contiene `iso`. */
export function lunediDi(iso: string): string {
  const dow = new Date(aUtc(iso)).getUTCDay(); // 0 = domenica
  const indietro = dow === 0 ? 6 : dow - 1;
  return sommaGiorni(iso, -indietro);
}

/** Le sette date della settimana, dal lunedì passato in ingresso. */
export function giorniDellaSettimana(lunedi: string): string[] {
  return Array.from({ length: 7 }, (_, i) => sommaGiorni(lunedi, i));
}
```

- [ ] **Step 10: Eseguire i test**

Run: `npm test`
Expected: PASS — smoke, 6 test di unità, 8 di date.

- [ ] **Step 11: Commit**

```bash
git add src/domain
git commit -m "feat(domain): tipi, sei aree fisse, conversione unità e aritmetica delle date"
```

---

## Task 3: `pantry` — residuo derivato e controllo a 90 giorni

**Files:**
- Create: `src/domain/pantry.ts`
- Test: `src/domain/__tests__/pantry.test.ts`

**Interfaces:**
- Consumes: `giorniTra` da `src/domain/date.ts`
- Produces: `nuovoResiduo(input): number`, `serveControllo(input): boolean`, `GIORNI_CONTROLLO_STAPLE = 90`

- [ ] **Step 1: Scrivere i test (falliranno)**

`src/domain/__tests__/pantry.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { nuovoResiduo, serveControllo, GIORNI_CONTROLLO_STAPLE } from '../pantry';

describe('nuovoResiduo', () => {
  it('somma il comprato e sottrae il consumato dal piano', () => {
    // Il caso yogurt: 50 g avanzati, due confezioni da 500, 750 g di porzioni.
    expect(nuovoResiduo({ residuoPrecedente: 50, acquistato: 1000, consumatoDaPiano: 750 })).toBe(300);
  });

  it('accumula il residuo su più settimane consecutive', () => {
    // 150 g al giorno per 5 giorni = 750 g, confezione da 500 g.
    let r = 0;
    r = nuovoResiduo({ residuoPrecedente: r, acquistato: 1000, consumatoDaPiano: 750 }); // 250
    r = nuovoResiduo({ residuoPrecedente: r, acquistato: 500, consumatoDaPiano: 750 });  // 0
    r = nuovoResiduo({ residuoPrecedente: r, acquistato: 1000, consumatoDaPiano: 750 }); // 250
    expect(r).toBe(250);
  });

  it('alza il residuo quando uno slot passa a fuori casa e non consuma', () => {
    expect(nuovoResiduo({ residuoPrecedente: 300, acquistato: 0, consumatoDaPiano: 0 })).toBe(300);
  });

  it('non scende sotto zero: una dispensa negativa non esiste', () => {
    expect(nuovoResiduo({ residuoPrecedente: 100, acquistato: 0, consumatoDaPiano: 400 })).toBe(0);
  });
});

describe('serveControllo', () => {
  const oggi = '2026-08-26';

  it('non chiede niente senza almeno un acquisto a storico', () => {
    expect(serveControllo({ ultimoAcquisto: null, ultimoCheck: null, oggi })).toBe(false);
  });

  it('non chiede niente prima dei 90 giorni', () => {
    expect(serveControllo({ ultimoAcquisto: '2026-06-01', ultimoCheck: null, oggi })).toBe(false);
  });

  it('chiede esattamente al novantesimo giorno', () => {
    expect(serveControllo({ ultimoAcquisto: '2026-05-28', ultimoCheck: null, oggi })).toBe(true);
  });

  it('chiede dopo il novantesimo giorno', () => {
    expect(serveControllo({ ultimoAcquisto: '2026-01-10', ultimoCheck: null, oggi })).toBe(true);
  });

  it('un "sì" recente fa ripartire il conto e zittisce il controllo', () => {
    expect(serveControllo({ ultimoAcquisto: '2026-01-10', ultimoCheck: '2026-08-01', oggi })).toBe(false);
  });

  it('torna a chiedere quando anche il "sì" ha novanta giorni', () => {
    expect(serveControllo({ ultimoAcquisto: '2026-01-10', ultimoCheck: '2026-05-01', oggi })).toBe(true);
  });

  it('usa la costante dichiarata, non un numero magico', () => {
    expect(GIORNI_CONTROLLO_STAPLE).toBe(90);
  });
});
```

- [ ] **Step 2: Eseguire i test per vederli fallire**

Run: `npx vitest run src/domain/__tests__/pantry.test.ts`
Expected: FAIL — `Failed to resolve import "../pantry"`.

- [ ] **Step 3: Implementare**

`src/domain/pantry.ts`:

```ts
import { giorniTra } from './date';

/**
 * Intervallo fisso del controllo staple. In Fase 1 non si apprende nulla:
 * la decisione chiusa il 2026-08-26 sostituisce la soglia `giorni_stimati × 0.8`
 * della regola 7 di list-builder.
 */
export const GIORNI_CONTROLLO_STAPLE = 90;

export interface NuovoResiduoInput {
  residuoPrecedente: number;
  acquistato: number;
  consumatoDaPiano: number;
}

/**
 * Il cuore del modello: la dispensa è derivata, mai inserita.
 * Si ferma a zero — un residuo negativo non ha significato fisico, e lasciarlo
 * negativo gonfierebbe in silenzio la lista successiva.
 */
export function nuovoResiduo(i: NuovoResiduoInput): number {
  return Math.max(0, i.residuoPrecedente + i.acquistato - i.consumatoDaPiano);
}

export interface ServeControlloInput {
  ultimoAcquisto: string | null;
  ultimoCheck: string | null;
  /** ISO yyyy-mm-dd */
  oggi: string;
}

/**
 * Vale solo per la classe `stima`; chi chiama filtra la classe.
 * Il conto riparte dal più recente fra l'ultimo acquisto e l'ultimo "sì":
 * senza questo, rispondere "sì" non zittirebbe mai il controllo.
 */
export function serveControllo(i: ServeControlloInput): boolean {
  if (!i.ultimoAcquisto) return false;
  const riferimento =
    i.ultimoCheck && i.ultimoCheck > i.ultimoAcquisto ? i.ultimoCheck : i.ultimoAcquisto;
  return giorniTra(riferimento, i.oggi) >= GIORNI_CONTROLLO_STAPLE;
}
```

- [ ] **Step 4: Eseguire i test**

Run: `npx vitest run src/domain/__tests__/pantry.test.ts`
Expected: PASS, 11 test.

- [ ] **Step 5: Commit**

```bash
git add src/domain/pantry.ts src/domain/__tests__/pantry.test.ts
git commit -m "feat(domain): residuo derivato e controllo staple a 90 giorni fissi"
```

---

## Task 4: `list-builder` — il cuore

**Files:**
- Create: `src/domain/list-builder.ts`
- Test: `src/domain/__tests__/list-builder.test.ts`
- Create: `src/domain/__tests__/fixtures.ts`

**Interfaces:**
- Consumes: tipi da `types.ts`, `convertiInUnitaBase` da `unita.ts`, `serveControllo` da `pantry.ts`, `AREE`/`nomeArea` da `aree.ts`
- Produces: `costruisciLista(input: ListaInput): ListaRisultato`, e i tipi `ListaInput`, `ListaRisultato`, `SezioneLista`, `VoceLista`, `VoceControllo`, `IngredienteMancanteError`. **Questi tipi sono il contratto che tutta la UI della lista consuma.**

- [ ] **Step 1: Scrivere le fixture**

`src/domain/__tests__/fixtures.ts`:

```ts
import type { Dish, Ingredient, MealSlot, PantryState, Impostazioni } from '../types';
import { ORDINE_AREE_DEFAULT } from '../aree';

export const yogurt: Ingredient = {
  id: 'yogurt', nome: 'Yogurt greco', unitaBase: 'g', area: 'latticini',
  classeResiduo: 'porzionabile', deperibile: true, formatoConfezione: 500,
};

export const avena: Ingredient = {
  id: 'avena', nome: "Fiocchi d'avena", unitaBase: 'g', area: 'cereali',
  classeResiduo: 'porzionabile', deperibile: false, formatoConfezione: 500,
};

export const uova: Ingredient = {
  id: 'uova', nome: 'Uova', unitaBase: 'pz', area: 'latticini',
  classeResiduo: 'intero', deperibile: true, formatoConfezione: 1,
};

export const olio: Ingredient = {
  id: 'olio', nome: 'Olio extravergine', unitaBase: 'ml', area: 'dispensa',
  classeResiduo: 'stima', deperibile: false, formatoConfezione: 1000,
};

export const passata: Ingredient = {
  id: 'passata', nome: 'Passata di pomodoro', unitaBase: 'g', area: 'dispensa',
  classeResiduo: 'porzionabile', deperibile: false, formatoConfezione: 700,
};

export const INGREDIENTI = [yogurt, avena, uova, olio, passata];

export const colazione: Dish = {
  id: 'colazione-yogurt', nome: 'Yogurt e avena', slotDefId: 'col',
  fonte: 'nutrizionista', attivo: true,
  ingredienti: [
    { ingredientId: 'yogurt', quantita: 150, unita: 'g' },
    { ingredientId: 'avena', quantita: 50, unita: 'g' },
  ],
};

export const frittata: Dish = {
  id: 'cena-frittata', nome: 'Frittata', slotDefId: 'cen',
  fonte: 'proprio', attivo: true,
  ingredienti: [
    { ingredientId: 'uova', quantita: 3, unita: 'pz' },
    { ingredientId: 'olio', quantita: 10, unita: 'ml' },
  ],
};

export const PIATTI = [colazione, frittata];

/** Cinque colazioni a casa, lunedì-venerdì. */
export function cinqueColazioni(): MealSlot[] {
  return ['2026-08-31', '2026-09-01', '2026-09-02', '2026-09-03', '2026-09-04'].map((data, i) => ({
    id: `col-${i}`, data, slotDefId: 'col', stato: 'casa' as const,
    dishId: 'colazione-yogurt', fonteStato: 'default' as const,
  }));
}

export const IMPOSTAZIONI: Impostazioni = {
  moltiplicatorePorzioni: 1,
  ordineAree: ORDINE_AREE_DEFAULT,
};

export function dispensaVuota(): PantryState[] {
  return INGREDIENTI.map((i) => ({
    ingredientId: i.id, residuo: 0, ultimoAcquisto: null,
    giorniStimati: 90, ultimoCheck: null,
  }));
}
```

- [ ] **Step 2: Scrivere i test (falliranno)**

`src/domain/__tests__/list-builder.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { costruisciLista, IngredienteMancanteError } from '../list-builder';
import { UnitaIncompatibileError } from '../unita';
import {
  INGREDIENTI, PIATTI, IMPOSTAZIONI, dispensaVuota, cinqueColazioni,
  yogurt, avena, colazione,
} from './fixtures';

const OGGI = '2026-08-30';

function base(over: Partial<Parameters<typeof costruisciLista>[0]> = {}) {
  return costruisciLista({
    slots: cinqueColazioni(),
    dishes: PIATTI,
    ingredients: INGREDIENTI,
    pantry: dispensaVuota(),
    impostazioni: IMPOSTAZIONI,
    oggi: OGGI,
    ...over,
  });
}

function voce(r: ReturnType<typeof costruisciLista>, id: string) {
  return [...r.base, ...r.topup].flatMap((s) => s.voci).find((v) => v.ingredientId === id);
}

describe('costruisciLista — aggregazione', () => {
  it('somma le porzioni degli slot a casa', () => {
    expect(voce(base(), 'yogurt')!.fabbisogno).toBe(750); // 150 × 5
  });

  it('ignora gli slot fuori casa: quel piatto non consuma', () => {
    const slots = cinqueColazioni();
    slots[0].stato = 'fuori';
    expect(voce(base({ slots }), 'yogurt')!.fabbisogno).toBe(600); // 150 × 4
  });

  it('ignora gli slot senza piatto assegnato', () => {
    const slots = cinqueColazioni();
    slots[0].dishId = null;
    expect(voce(base({ slots }), 'yogurt')!.fabbisogno).toBe(600);
  });

  it('applica il moltiplicatore porzioni per cucinare in due', () => {
    const impostazioni = { ...IMPOSTAZIONI, moltiplicatorePorzioni: 2 };
    expect(voce(base({ impostazioni }), 'yogurt')!.fabbisogno).toBe(1500);
  });

  it('normalizza le unità miste sullo stesso ingrediente', () => {
    const dishes = [{
      ...colazione,
      ingredienti: [
        { ingredientId: 'yogurt', quantita: 150, unita: 'g' as const },
        { ingredientId: 'yogurt', quantita: 0.1, unita: 'kg' as const },
      ],
    }];
    expect(voce(base({ dishes }), 'yogurt')!.fabbisogno).toBe(1250); // (150 + 100) × 5
  });

  it('rifiuta un\'unità incompatibile invece di indovinare', () => {
    const dishes = [{
      ...colazione,
      ingredienti: [{ ingredientId: 'uova', quantita: 100, unita: 'g' as const }],
    }];
    expect(() => base({ dishes })).toThrow(UnitaIncompatibileError);
  });

  it('segnala un ingrediente che il piatto cita ma che non esiste', () => {
    const dishes = [{
      ...colazione,
      ingredienti: [{ ingredientId: 'fantasma', quantita: 1, unita: 'g' as const }],
    }];
    expect(() => base({ dishes })).toThrow(IngredienteMancanteError);
  });
});

describe('costruisciLista — residuo e confezioni', () => {
  it('sottrae il residuo dal fabbisogno', () => {
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'yogurt' ? { ...p, residuo: 50 } : p);
    const v = voce(base({ pantry }), 'yogurt')!;
    expect(v.fabbisogno).toBe(750);
    expect(v.residuo).toBe(50);
    expect(v.daComprare).toBe(700);
  });

  it('arrotonda per eccesso a confezioni intere', () => {
    const v = voce(base(), 'yogurt')!;
    expect(v.daComprare).toBe(750);
    expect(v.confezioni).toBe(2);       // ceil(750 / 500)
    expect(v.quantitaTotale).toBe(1000);
  });

  it('calcola il residuo che resterà a fine settimana', () => {
    // Il caso yogurt della spec: 50 in casa, servono 750, compri 1000 → restano 300.
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'yogurt' ? { ...p, residuo: 50 } : p);
    expect(voce(base({ pantry }), 'yogurt')!.residuoPrevisto).toBe(300);
  });

  it('sparisce dalla lista quando il residuo copre già tutto', () => {
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'avena' ? { ...p, residuo: 900 } : p);
    expect(voce(base({ pantry }), 'avena')).toBeUndefined();
  });

  it('conta la classe intero a pezzi, con formato uno', () => {
    const slots = [{
      id: 'c1', data: '2026-08-31', slotDefId: 'cen', stato: 'casa' as const,
      dishId: 'cena-frittata', fonteStato: 'default' as const,
    }];
    const v = voce(base({ slots }), 'uova')!;
    expect(v.confezioni).toBe(3);
    expect(v.quantitaTotale).toBe(3);
    expect(v.unita).toBe('pz');
  });

  it('non mette mai in lista un ingrediente di classe stima', () => {
    const slots = [{
      id: 'c1', data: '2026-08-31', slotDefId: 'cen', stato: 'casa' as const,
      dishId: 'cena-frittata', fonteStato: 'default' as const,
    }];
    expect(voce(base({ slots }), 'olio')).toBeUndefined();
  });
});

describe('costruisciLista — controlli staple', () => {
  it('chiede dell\'olio dopo novanta giorni, anche se non è in nessun piatto', () => {
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'olio' ? { ...p, ultimoAcquisto: '2026-01-10' } : p);
    const controlli = base({ pantry }).base.flatMap((s) => s.controlli);
    expect(controlli.map((c) => c.ingredientId)).toEqual(['olio']);
  });

  it('non chiede niente prima dei novanta giorni', () => {
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'olio' ? { ...p, ultimoAcquisto: '2026-08-01' } : p);
    expect(base({ pantry }).base.flatMap((s) => s.controlli)).toHaveLength(0);
  });

  it('mette il controllo nella lista che compete al suo ingrediente', () => {
    const pantry = dispensaVuota().map((p) =>
      p.ingredientId === 'olio' ? { ...p, ultimoAcquisto: '2026-01-10' } : p);
    const r = base({ pantry });
    expect(r.topup.flatMap((s) => s.controlli)).toHaveLength(0); // olio non è deperibile
    expect(r.base.flatMap((s) => s.controlli)).toHaveLength(1);
  });
});

describe('costruisciLista — split e ordinamento', () => {
  it('manda i deperibili nel top-up e il resto nella base', () => {
    const r = base();
    expect(r.topup.flatMap((s) => s.voci).map((v) => v.ingredientId)).toEqual(['yogurt']);
    expect(r.base.flatMap((s) => s.voci).map((v) => v.ingredientId)).toEqual(['avena']);
  });

  it('ordina le sezioni secondo l\'ordine dei reparti scelto dall\'utente', () => {
    const impostazioni = {
      ...IMPOSTAZIONI,
      ordineAree: ['surgelati', 'dispensa', 'cereali', 'latticini', 'macelleria', 'ortofrutta'] as const,
    };
    const pantry = dispensaVuota();
    const slots = [
      ...cinqueColazioni(),
      { id: 'c1', data: '2026-08-31', slotDefId: 'cen', stato: 'casa' as const,
        dishId: 'cena-frittata', fonteStato: 'default' as const },
    ];
    const r = costruisciLista({
      slots, dishes: PIATTI, ingredients: INGREDIENTI, pantry,
      impostazioni: { ...impostazioni, ordineAree: [...impostazioni.ordineAree] },
      oggi: OGGI,
    });
    expect(r.base.map((s) => s.area)).toEqual(['cereali']);
    expect(r.topup.map((s) => s.area)).toEqual(['latticini']);
  });

  it('non produce sezioni vuote', () => {
    const r = base();
    expect([...r.base, ...r.topup].every((s) => s.voci.length + s.controlli.length > 0)).toBe(true);
  });

  it('mette per prima la voce con più confezioni, così la protagonista è l\'acquisto più grosso', () => {
    const slots = [
      ...cinqueColazioni(),
      { id: 'c1', data: '2026-08-31', slotDefId: 'cen', stato: 'casa' as const,
        dishId: 'cena-frittata', fonteStato: 'default' as const },
    ];
    const sezione = base({ slots }).topup.find((s) => s.area === 'latticini')!;
    expect(sezione.voci[0].ingredientId).toBe('yogurt'); // 2 confezioni contro 3 pezzi… vedi nota
  });
});
```

> Nota sull'ultimo test: `uova` ha 3 confezioni e `yogurt` 2, quindi l'ordinamento per confezioni decrescenti mette `uova` per prima. **Correggere l'aspettativa a `'uova'` quando si scrive il test** — è il comportamento corretto, e questa nota resta come promemoria che l'ordinamento è per confezioni, non per importanza percepita.

- [ ] **Step 3: Eseguire i test per vederli fallire**

Run: `npx vitest run src/domain/__tests__/list-builder.test.ts`
Expected: FAIL — `Failed to resolve import "../list-builder"`.

- [ ] **Step 4: Implementare**

`src/domain/list-builder.ts`:

```ts
import type {
  AreaId, Dish, Impostazioni, Ingredient, MealSlot, PantryState, UnitaBase,
} from './types';
import { convertiInUnitaBase } from './unita';
import { serveControllo } from './pantry';

export class IngredienteMancanteError extends Error {
  constructor(id: string) {
    super(`Un piatto cita l'ingrediente ${id}, che non esiste nel repertorio.`);
    this.name = 'IngredienteMancanteError';
  }
}

export interface VoceLista {
  ingredientId: string;
  nome: string;
  area: AreaId;
  unita: UnitaBase;
  /** Somma delle porzioni degli slot a casa, in unita. */
  fabbisogno: number;
  /** Quanto c'è già in casa, derivato. */
  residuo: number;
  daComprare: number;
  confezioni: number;
  /** confezioni × formatoConfezione */
  quantitaTotale: number;
  /** residuo + quantitaTotale − fabbisogno: il residuo iniziale della settimana dopo. */
  residuoPrevisto: number;
  /** Il sottotitolo "serve X · in casa Y" si mostra solo quando è true. */
  mostraDettaglio: boolean;
}

export interface VoceControllo {
  ingredientId: string;
  nome: string;
  area: AreaId;
  /** Confezione da comprare se l'utente risponde "no". */
  formatoConfezione: number;
  unita: UnitaBase;
}

export interface SezioneLista {
  area: AreaId;
  voci: VoceLista[];
  controlli: VoceControllo[];
}

export interface ListaRisultato {
  base: SezioneLista[];
  topup: SezioneLista[];
}

export interface ListaInput {
  slots: MealSlot[];
  dishes: Dish[];
  ingredients: Ingredient[];
  pantry: PantryState[];
  impostazioni: Impostazioni;
  /** ISO yyyy-mm-dd */
  oggi: string;
}

/**
 * Funzione pura: niente rete, niente LLM, niente DB.
 * Segue alla lettera le nove regole della sezione "Regole di list-builder"
 * della spec, con la regola 7 sostituita dai 90 giorni fissi.
 */
export function costruisciLista(input: ListaInput): ListaRisultato {
  const { slots, dishes, ingredients, pantry, impostazioni, oggi } = input;

  const perId = new Map(ingredients.map((i) => [i.id, i]));
  const piattoPerId = new Map(dishes.map((d) => [d.id, d]));
  const dispensaPerId = new Map(pantry.map((p) => [p.ingredientId, p]));

  // Regole 1-3: solo gli slot a casa, espansi in ingredienti e aggregati.
  const fabbisogni = new Map<string, number>();
  for (const slot of slots) {
    if (slot.stato !== 'casa' || !slot.dishId) continue;
    const piatto = piattoPerId.get(slot.dishId);
    if (!piatto) continue;
    for (const riga of piatto.ingredienti) {
      const ing = perId.get(riga.ingredientId);
      if (!ing) throw new IngredienteMancanteError(riga.ingredientId);
      const q = convertiInUnitaBase(riga.quantita, riga.unita, ing.unitaBase)
        * impostazioni.moltiplicatorePorzioni;
      fabbisogni.set(ing.id, (fabbisogni.get(ing.id) ?? 0) + q);
    }
  }

  // Regole 4-6: residuo, confezioni, classe intero.
  const voci: VoceLista[] = [];
  for (const [ingredientId, fabbisogno] of fabbisogni) {
    const ing = perId.get(ingredientId)!;
    if (ing.classeResiduo === 'stima') continue; // regola 7: nessuna aritmetica
    const residuo = dispensaPerId.get(ingredientId)?.residuo ?? 0;
    const daComprare = Math.max(0, fabbisogno - residuo);
    const formato = ing.classeResiduo === 'intero' ? 1 : ing.formatoConfezione;
    const confezioni = Math.ceil(daComprare / formato);
    if (confezioni === 0) continue; // il residuo copre già tutto
    const quantitaTotale = confezioni * formato;
    voci.push({
      ingredientId, nome: ing.nome, area: ing.area, unita: ing.unitaBase,
      fabbisogno, residuo, daComprare, confezioni, quantitaTotale,
      residuoPrevisto: residuo + quantitaTotale - fabbisogno,
      mostraDettaglio: ing.classeResiduo === 'porzionabile',
    });
  }

  // Regola 7: controlli sugli staple, indipendenti dal piano della settimana.
  const controlli: VoceControllo[] = [];
  for (const ing of ingredients) {
    if (ing.classeResiduo !== 'stima') continue;
    const p = dispensaPerId.get(ing.id);
    if (!p) continue;
    if (!serveControllo({ ultimoAcquisto: p.ultimoAcquisto, ultimoCheck: p.ultimoCheck, oggi })) {
      continue;
    }
    controlli.push({
      ingredientId: ing.id, nome: ing.nome, area: ing.area,
      formatoConfezione: ing.formatoConfezione, unita: ing.unitaBase,
    });
  }

  // Regole 8-9: split per deperibilità, sezioni nell'ordine dei reparti.
  const deperibile = (id: string) => perId.get(id)!.deperibile;
  return {
    base: sezioni(
      voci.filter((v) => !deperibile(v.ingredientId)),
      controlli.filter((c) => !deperibile(c.ingredientId)),
      impostazioni.ordineAree,
    ),
    topup: sezioni(
      voci.filter((v) => deperibile(v.ingredientId)),
      controlli.filter((c) => deperibile(c.ingredientId)),
      impostazioni.ordineAree,
    ),
  };
}

function sezioni(voci: VoceLista[], controlli: VoceControllo[], ordine: AreaId[]): SezioneLista[] {
  const out: SezioneLista[] = [];
  for (const area of ordine) {
    const v = voci
      .filter((x) => x.area === area)
      .sort((a, b) => b.confezioni - a.confezioni || a.nome.localeCompare(b.nome, 'it'));
    const c = controlli
      .filter((x) => x.area === area)
      .sort((a, b) => a.nome.localeCompare(b.nome, 'it'));
    if (v.length === 0 && c.length === 0) continue; // niente sezioni vuote
    out.push({ area, voci: v, controlli: c });
  }
  return out;
}
```

- [ ] **Step 5: Eseguire i test**

Run: `npx vitest run src/domain/__tests__/list-builder.test.ts`
Expected: PASS. Se l'ultimo test dell'ordinamento fallisce con `'uova'` ricevuto contro `'yogurt'` atteso, correggere l'**aspettativa** del test a `'uova'` (vedi la nota allo Step 2) e rieseguire.

- [ ] **Step 6: Verificare che il dominio non abbia dipendenze**

Run: `grep -rE "from '(next|react|@supabase)" src/domain/`
Expected: nessun risultato. Il dominio deve restare puro.

- [ ] **Step 7: Commit**

```bash
git add src/domain/list-builder.ts src/domain/__tests__/
git commit -m "feat(domain): list-builder, la funzione pura che costruisce la lista"
```

---

## Task 5: `week-shape` e `planner` — la forma della settimana

**Files:**
- Create: `src/domain/week-shape.ts`
- Create: `src/domain/planner.ts`
- Test: `src/domain/__tests__/week-shape.test.ts`, `src/domain/__tests__/planner.test.ts`

**Interfaces:**
- Consumes: tipi da `types.ts`, `giorniDellaSettimana` da `date.ts`
- Produces: `generaSettimana(input): MealSlot[]`, `applicaStato(slot, stato, fonte): MealSlot`, `PRIORITA_FONTE`, `assegnaPiatti(input): MealSlot[]`

- [ ] **Step 1: Scrivere i test di `week-shape` (falliranno)**

`src/domain/__tests__/week-shape.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { generaSettimana, applicaStato } from '../week-shape';
import type { MealSlot, MealSlotDef } from '../types';

const SLOT_DEFS: MealSlotDef[] = [
  { id: 'col', nome: 'Colazione', posizione: 0, assenzeAbituali: [false, false, true, false, false, false, false] },
  { id: 'pra', nome: 'Pranzo', posizione: 1, assenzeAbituali: [true, true, true, true, true, false, false] },
  { id: 'cen', nome: 'Cena', posizione: 2, assenzeAbituali: [false, false, false, false, false, false, false] },
];

describe('generaSettimana', () => {
  const slots = generaSettimana({ dataInizio: '2026-08-31', slotDefs: SLOT_DEFS });

  it('produce sette giorni per ogni pasto definito', () => {
    expect(slots).toHaveLength(21);
  });

  it('regge tre pasti come cinque: niente enum a quattro slot', () => {
    const cinque = [...SLOT_DEFS,
      { id: 'spu1', nome: 'Spuntino', posizione: 3, assenzeAbituali: Array(7).fill(false) },
      { id: 'spu2', nome: 'Merenda', posizione: 4, assenzeAbituali: Array(7).fill(false) },
    ];
    expect(generaSettimana({ dataInizio: '2026-08-31', slotDefs: cinque })).toHaveLength(35);
  });

  it('mette a casa di default', () => {
    const lun = slots.find((s) => s.data === '2026-08-31' && s.slotDefId === 'cen')!;
    expect(lun.stato).toBe('casa');
  });

  it('applica le assenze abituali: il pranzo infrasettimanale è fuori', () => {
    const mar = slots.find((s) => s.data === '2026-09-01' && s.slotDefId === 'pra')!;
    expect(mar.stato).toBe('fuori');
  });

  it('non applica l\'assenza abituale nel weekend se non è segnata', () => {
    const sab = slots.find((s) => s.data === '2026-09-05' && s.slotDefId === 'pra')!;
    expect(sab.stato).toBe('casa');
  });

  it('marca ogni slot come proveniente dal default', () => {
    expect(slots.every((s) => s.fonteStato === 'default')).toBe(true);
  });

  it('non assegna nessun piatto: lo fa il planner', () => {
    expect(slots.every((s) => s.dishId === null)).toBe(true);
  });
});

describe('applicaStato', () => {
  const slot: MealSlot = {
    id: 's1', data: '2026-08-31', slotDefId: 'cen',
    stato: 'casa', dishId: null, fonteStato: 'checkin',
  };

  it('una fonte più forte sovrascrive', () => {
    expect(applicaStato(slot, 'fuori', 'correzione').stato).toBe('fuori');
  });

  it('la stessa fonte sovrascrive: correggersi due volte deve funzionare', () => {
    expect(applicaStato(slot, 'fuori', 'checkin').stato).toBe('fuori');
  });

  it('una fonte più debole non tocca niente', () => {
    const dopo = applicaStato(slot, 'fuori', 'default');
    expect(dopo.stato).toBe('casa');
    expect(dopo.fonteStato).toBe('checkin');
  });

  it('registra la nuova fonte quando scrive', () => {
    expect(applicaStato(slot, 'fuori', 'correzione').fonteStato).toBe('correzione');
  });
});
```

- [ ] **Step 2: Eseguire i test per vederli fallire**

Run: `npx vitest run src/domain/__tests__/week-shape.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 3: Implementare `week-shape`**

`src/domain/week-shape.ts`:

```ts
import type { FonteStato, MealSlot, MealSlotDef, StatoSlot } from './types';
import { giorniDellaSettimana } from './date';

/** correzione > check-in > calendario > default. In Fase 1 esistono solo gli estremi. */
export const PRIORITA_FONTE: Record<FonteStato, number> = {
  default: 0,
  calendario: 1,
  checkin: 2,
  correzione: 3,
};

export interface GeneraSettimanaInput {
  /** Lunedì della settimana, ISO yyyy-mm-dd. */
  dataInizio: string;
  /** Da 3 a 5, già ordinati per posizione. */
  slotDefs: MealSlotDef[];
}

/**
 * Il default della settimana: ogni pasto è a casa, tranne dove l'utente ha
 * dichiarato un'assenza abituale nelle Impostazioni. Nessun piatto assegnato.
 */
export function generaSettimana(input: GeneraSettimanaInput): MealSlot[] {
  const giorni = giorniDellaSettimana(input.dataInizio);
  const slots: MealSlot[] = [];
  for (const def of input.slotDefs) {
    giorni.forEach((data, indiceGiorno) => {
      const fuori = def.assenzeAbituali[indiceGiorno] === true;
      slots.push({
        id: `${data}:${def.id}`,
        data,
        slotDefId: def.id,
        stato: fuori ? 'fuori' : 'casa',
        dishId: null,
        fonteStato: 'default',
      });
    });
  }
  return slots;
}

/**
 * Scrive lo stato solo se la fonte è almeno forte quanto quella già registrata.
 * Serve dalla Fase 2 in poi, quando il calendario proporrà stati che il
 * check-in dell'utente non deve vedersi sovrascrivere.
 */
export function applicaStato(slot: MealSlot, stato: StatoSlot, fonte: FonteStato): MealSlot {
  if (PRIORITA_FONTE[fonte] < PRIORITA_FONTE[slot.fonteStato]) return slot;
  return { ...slot, stato, fonteStato: fonte };
}
```

- [ ] **Step 4: Eseguire i test**

Run: `npx vitest run src/domain/__tests__/week-shape.test.ts`
Expected: PASS, 11 test.

- [ ] **Step 5: Scrivere i test del planner (falliranno)**

`src/domain/__tests__/planner.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { assegnaPiatti } from '../planner';
import { generaSettimana } from '../week-shape';
import type { Dish, MealSlotDef } from '../types';

const DEFS: MealSlotDef[] = [
  { id: 'col', nome: 'Colazione', posizione: 0, assenzeAbituali: [false, false, true, false, false, false, false] },
  { id: 'cen', nome: 'Cena', posizione: 1, assenzeAbituali: Array(7).fill(false) },
];

function piatto(id: string, slotDefId: string, attivo = true): Dish {
  return { id, nome: id, slotDefId, fonte: 'proprio', attivo, ingredienti: [] };
}

const PIATTI = [piatto('c1', 'col'), piatto('c2', 'col'), piatto('n1', 'cen')];

describe('assegnaPiatti', () => {
  const slots = generaSettimana({ dataInizio: '2026-08-31', slotDefs: DEFS });

  it('assegna un piatto a ogni slot a casa', () => {
    const dopo = assegnaPiatti({ slots, dishes: PIATTI });
    expect(dopo.filter((s) => s.stato === 'casa').every((s) => s.dishId !== null)).toBe(true);
  });

  it('non assegna niente agli slot fuori casa', () => {
    const dopo = assegnaPiatti({ slots, dishes: PIATTI });
    expect(dopo.filter((s) => s.stato === 'fuori').every((s) => s.dishId === null)).toBe(true);
  });

  it('ruota fra i piatti disponibili per quel pasto', () => {
    const dopo = assegnaPiatti({ slots, dishes: PIATTI });
    const col = dopo.filter((s) => s.slotDefId === 'col' && s.stato === 'casa')
      .sort((a, b) => a.data.localeCompare(b.data));
    expect(col.map((s) => s.dishId)).toEqual(['c1', 'c2', 'c1', 'c2', 'c1', 'c2']);
  });

  it('non pesca piatti di un altro pasto', () => {
    const dopo = assegnaPiatti({ slots, dishes: PIATTI });
    expect(dopo.filter((s) => s.slotDefId === 'cen' && s.stato === 'casa')
      .every((s) => s.dishId === 'n1')).toBe(true);
  });

  it('ignora i piatti disattivati', () => {
    const dopo = assegnaPiatti({ slots, dishes: [piatto('c1', 'col'), piatto('c2', 'col', false), piatto('n1', 'cen')] });
    expect(dopo.filter((s) => s.slotDefId === 'col' && s.stato === 'casa')
      .every((s) => s.dishId === 'c1')).toBe(true);
  });

  it('lascia lo slot vuoto se per quel pasto non c\'è nessun piatto', () => {
    const dopo = assegnaPiatti({ slots, dishes: [piatto('n1', 'cen')] });
    expect(dopo.filter((s) => s.slotDefId === 'col').every((s) => s.dishId === null)).toBe(true);
  });

  it('non tocca uno slot che ha già un piatto scelto a mano', () => {
    const conScelta = slots.map((s) =>
      s.data === '2026-08-31' && s.slotDefId === 'col' ? { ...s, dishId: 'c2' } : s);
    const dopo = assegnaPiatti({ slots: conScelta, dishes: PIATTI });
    expect(dopo.find((s) => s.data === '2026-08-31' && s.slotDefId === 'col')!.dishId).toBe('c2');
  });
});
```

- [ ] **Step 6: Eseguire i test per vederli fallire**

Run: `npx vitest run src/domain/__tests__/planner.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 7: Implementare il planner**

`src/domain/planner.ts`:

```ts
import type { Dish, MealSlot } from './types';

export interface AssegnaPiattiInput {
  slots: MealSlot[];
  dishes: Dish[];
}

/**
 * La rotazione della spec: deterministica, senza storico e senza preferenze.
 * Assegna solo agli slot a casa ancora vuoti — una scelta fatta a mano dalla
 * schermata "Scegli il piatto" non viene mai sovrascritta.
 */
export function assegnaPiatti(input: AssegnaPiattiInput): MealSlot[] {
  const giorni = [...new Set(input.slots.map((s) => s.data))].sort();
  const indiceGiorno = new Map(giorni.map((d, i) => [d, i]));

  const perSlotDef = new Map<string, Dish[]>();
  for (const d of input.dishes) {
    if (!d.attivo) continue;
    const arr = perSlotDef.get(d.slotDefId) ?? [];
    arr.push(d);
    perSlotDef.set(d.slotDefId, arr);
  }

  return input.slots.map((slot) => {
    if (slot.stato !== 'casa' || slot.dishId !== null) return slot;
    const candidati = perSlotDef.get(slot.slotDefId);
    if (!candidati || candidati.length === 0) return slot;
    const i = indiceGiorno.get(slot.data) ?? 0;
    return { ...slot, dishId: candidati[i % candidati.length].id };
  });
}
```

- [ ] **Step 8: Eseguire tutti i test**

Run: `npm test`
Expected: PASS su tutto.

- [ ] **Step 9: Commit**

```bash
git add src/domain
git commit -m "feat(domain): forma della settimana, precedenza delle fonti e rotazione dei piatti"
```

---

## Task 6: Schema Supabase con RLS

**Files:**
- Create: `supabase/migrations/0001_schema.sql`
- Create: `supabase/migrations/0002_rls.sql`
- Create: `supabase/seed.sql`
- Modify: `.env.local.example`

**Interfaces:**
- Consumes: gli `AreaId`, `ClasseResiduo`, `StatoSlot`, `FonteStato` del Task 2 — i `check` SQL devono elencare esattamente gli stessi valori
- Produces: lo schema che il Task 7 mappa in tipi di dominio

- [ ] **Step 1: Creare il progetto Supabase e prendere le chiavi**

Creare un progetto su supabase.com (region EU, Andrea è a Milano). Copiare URL e anon key in `.env.local`:

```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

`.env.local.example` contiene le stesse due righe con i valori vuoti. `.env.local` è già in `.gitignore`.

- [ ] **Step 2: Scrivere lo schema**

`supabase/migrations/0001_schema.sql`:

```sql
-- Sei aree fisse: vivono come costanti in src/domain/aree.ts, non come tabella.
-- Qui compaiono solo come vincolo di dominio.

create table meal_slot_def (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  nome text not null,
  posizione int not null,
  -- Sette booleani, indice 0 = lunedì. true = abitualmente fuori casa.
  assenze_abituali boolean[] not null default '{false,false,false,false,false,false,false}',
  created_at timestamptz not null default now(),
  constraint assenze_sette check (array_length(assenze_abituali, 1) = 7)
);
-- Il vincolo "da 3 a 5 pasti" è applicativo: in SQL richiederebbe un trigger
-- che conta le righe per utente, e il costo non vale la copertura.

create table ingredient (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  nome text not null,
  unita_base text not null check (unita_base in ('g', 'ml', 'pz')),
  area text not null check (area in
    ('ortofrutta', 'macelleria', 'latticini', 'cereali', 'dispensa', 'surgelati')),
  classe_residuo text not null check (classe_residuo in ('porzionabile', 'intero', 'stima')),
  deperibile boolean not null default false,
  formato_confezione numeric not null check (formato_confezione > 0),
  -- Previste per il futuro, mai popolate né lette in v1.
  kcal_100 numeric, prot_100 numeric, carb_100 numeric, gras_100 numeric,
  created_at timestamptz not null default now()
);

create table dish (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  nome text not null,
  slot_def_id uuid not null references meal_slot_def(id) on delete cascade,
  fonte text not null default 'proprio' check (fonte in ('nutrizionista', 'proprio')),
  attivo boolean not null default true,
  created_at timestamptz not null default now()
);

create table dish_ingredient (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  dish_id uuid not null references dish(id) on delete cascade,
  ingredient_id uuid not null references ingredient(id) on delete restrict,
  quantita numeric not null check (quantita > 0),
  unita text not null check (unita in ('g', 'kg', 'ml', 'l', 'pz')),
  unique (dish_id, ingredient_id)
);

create table week (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  data_inizio date not null,
  stato text not null default 'bozza' check (stato in ('bozza', 'confermata', 'chiusa')),
  created_at timestamptz not null default now(),
  unique (user_id, data_inizio)
);

create table meal_slot (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  week_id uuid not null references week(id) on delete cascade,
  data date not null,
  slot_def_id uuid not null references meal_slot_def(id) on delete cascade,
  stato text not null check (stato in ('casa', 'fuori', 'saltato')),
  dish_id uuid references dish(id) on delete set null,
  fonte_stato text not null default 'default'
    check (fonte_stato in ('default', 'calendario', 'checkin', 'correzione')),
  unique (week_id, data, slot_def_id)
);

create table shopping_list (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  week_id uuid not null references week(id) on delete cascade,
  tipo text not null check (tipo in ('base', 'topup')),
  creata_il timestamptz not null default now(),
  chiusa_il timestamptz,
  unique (week_id, tipo)
);

create table shopping_list_item (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  shopping_list_id uuid not null references shopping_list(id) on delete cascade,
  ingredient_id uuid not null references ingredient(id) on delete restrict,
  -- Congelati al momento della generazione: la lista non si ricalcola sotto i piedi
  -- di chi è in corsia.
  fabbisogno numeric not null,
  residuo numeric not null,
  confezioni int not null,
  quantita_totale numeric not null,
  unita text not null check (unita in ('g', 'ml', 'pz')),
  area text not null,
  spuntato boolean not null default false,
  spuntato_il timestamptz,
  origine text not null default 'piano' check (origine in ('piano', 'controllo', 'manuale')),
  unique (shopping_list_id, ingredient_id)
);

create table pantry_state (
  ingredient_id uuid primary key references ingredient(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  residuo numeric not null default 0 check (residuo >= 0),
  ultimo_acquisto date,
  giorni_stimati int not null default 90,
  ultimo_check date
);

-- Storico degli acquisti. La spec impone di registrarli fin dalla v1 perché la
-- Fase 4 possa imparare i ritmi; pantry_state.ultimo_acquisto tiene solo l'ultimo.
create table purchase (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  ingredient_id uuid not null references ingredient(id) on delete cascade,
  data date not null,
  confezioni int not null,
  quantita numeric not null,
  shopping_list_id uuid references shopping_list(id) on delete set null
);

create table settings (
  user_id uuid primary key references auth.users(id) on delete cascade,
  moltiplicatore_porzioni int not null default 1 check (moltiplicatore_porzioni between 1 and 6),
  ordine_aree text[] not null default
    '{ortofrutta,macelleria,latticini,cereali,dispensa,surgelati}',
  constraint ordine_sei check (array_length(ordine_aree, 1) = 6)
);

create index on dish (user_id, slot_def_id) where attivo;
create index on meal_slot (week_id, data);
create index on shopping_list_item (shopping_list_id);
create index on purchase (user_id, ingredient_id, data desc);
```

- [ ] **Step 3: Scrivere le policy RLS**

`supabase/migrations/0002_rls.sql`:

```sql
-- user_id su ogni tabella, RLS su ogni tabella. Nessuna eccezione: le tabelle
-- figlie portano user_id anche se sarebbe deducibile per join, così ogni policy
-- è identica e non c'è modo di sbagliarne una.

do $$
declare t text;
begin
  foreach t in array array[
    'meal_slot_def', 'ingredient', 'dish', 'dish_ingredient', 'week',
    'meal_slot', 'shopping_list', 'shopping_list_item', 'pantry_state',
    'purchase', 'settings'
  ] loop
    execute format('alter table %I enable row level security', t);
    execute format('alter table %I force row level security', t);
    execute format(
      'create policy %I on %I for all to authenticated using (auth.uid() = user_id) with check (auth.uid() = user_id)',
      t || '_proprietario', t
    );
  end loop;
end $$;
```

- [ ] **Step 4: Applicare le migrazioni**

Incollare i due file nell'SQL Editor di Supabase ed eseguirli in ordine, oppure con la CLI:

```bash
npx supabase db push
```

- [ ] **Step 5: Verificare che RLS morda davvero**

Nell'SQL Editor, con il ruolo `anon`:

```sql
set role anon;
select * from ingredient;
```

Expected: zero righe (non un errore di permessi — RLS filtra, non rifiuta). Poi `reset role;`.

- [ ] **Step 6: Scrivere il seed dei pasti di default**

`supabase/seed.sql` — quattro pasti con le assenze abituali di Andrea (pranzo fuori dal lunedì al venerdì), da eseguire una volta dopo la registrazione sostituendo l'uuid:

```sql
-- Sostituire :uid con l'uuid dell'utente, leggibile da auth.users.
insert into meal_slot_def (user_id, nome, posizione, assenze_abituali) values
  (:'uid', 'Colazione', 0, '{false,false,false,false,false,false,false}'),
  (:'uid', 'Spuntino',  1, '{false,false,false,false,false,true,true}'),
  (:'uid', 'Pranzo',    2, '{true,true,true,true,true,false,false}'),
  (:'uid', 'Cena',      3, '{false,false,false,false,false,false,false}');

insert into settings (user_id) values (:'uid');
```

- [ ] **Step 7: Commit**

```bash
git add supabase .env.local.example
git commit -m "feat(db): schema, RLS su ogni tabella e seed dei pasti"
```

---

## Task 7: Data layer e autenticazione

**Files:**
- Create: `src/data/supabase.ts`, `src/data/mappers.ts`
- Create: `src/data/repertorio.ts`, `src/data/settimana.ts`, `src/data/lista.ts`, `src/data/dispensa.ts`, `src/data/impostazioni.ts`
- Create: `src/app/entra/page.tsx`, `src/middleware.ts`
- Test: `src/data/__tests__/mappers.test.ts`

**Interfaces:**
- Consumes: tutti i tipi del Task 2, `ListaRisultato` dal Task 4
- Produces: funzioni asincrone che restituiscono **tipi di dominio**, mai righe grezze. Le firme esatte sono allo Step 7; ogni task successivo si aspetta queste e non altre.

- [ ] **Step 1: Installare i pacchetti Supabase**

```bash
npm install @supabase/supabase-js @supabase/ssr
```

- [ ] **Step 2: Scrivere il client**

`src/data/supabase.ts`:

```ts
import { createBrowserClient } from '@supabase/ssr';

export function client() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
  );
}
```

- [ ] **Step 3: Scrivere il test dei mapper (fallirà)**

I mapper sono l'unica parte del data layer che si testa in isolamento: convertono `snake_case` del DB in `camelCase` di dominio, ed è lì che nascono i bug silenziosi.

`src/data/__tests__/mappers.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { aIngrediente, aMealSlot, aPantryState } from '../mappers';

describe('aIngrediente', () => {
  it('converte una riga in un ingrediente di dominio', () => {
    expect(aIngrediente({
      id: 'a', nome: 'Yogurt greco', unita_base: 'g', area: 'latticini',
      classe_residuo: 'porzionabile', deperibile: true, formato_confezione: '500',
    })).toEqual({
      id: 'a', nome: 'Yogurt greco', unitaBase: 'g', area: 'latticini',
      classeResiduo: 'porzionabile', deperibile: true, formatoConfezione: 500,
    });
  });

  it('converte i numeric di Postgres, che arrivano come stringhe', () => {
    expect(aIngrediente({
      id: 'a', nome: 'x', unita_base: 'g', area: 'dispensa',
      classe_residuo: 'porzionabile', deperibile: false, formato_confezione: '0.5',
    }).formatoConfezione).toBe(0.5);
  });
});

describe('aMealSlot', () => {
  it('normalizza la data a yyyy-mm-dd', () => {
    expect(aMealSlot({
      id: 's', data: '2026-08-31', slot_def_id: 'col',
      stato: 'casa', dish_id: null, fonte_stato: 'default',
    }).data).toBe('2026-08-31');
  });

  it('tiene dishId a null quando non c\'è piatto', () => {
    expect(aMealSlot({
      id: 's', data: '2026-08-31', slot_def_id: 'col',
      stato: 'fuori', dish_id: null, fonte_stato: 'checkin',
    }).dishId).toBeNull();
  });
});

describe('aPantryState', () => {
  it('porta residuo e date nel dominio', () => {
    expect(aPantryState({
      ingredient_id: 'a', residuo: '300', ultimo_acquisto: '2026-01-10',
      giorni_stimati: 90, ultimo_check: null,
    })).toEqual({
      ingredientId: 'a', residuo: 300, ultimoAcquisto: '2026-01-10',
      giorniStimati: 90, ultimoCheck: null,
    });
  });
});
```

- [ ] **Step 4: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/data/__tests__/mappers.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 5: Implementare i mapper**

`src/data/mappers.ts`:

```ts
import type {
  AreaId, ClasseResiduo, FonteStato, Ingredient, MealSlot,
  MealSlotDef, PantryState, StatoSlot, UnitaBase, UnitaMisura,
} from '@/domain/types';

/** Postgres restituisce i numeric come stringa: convertire sempre, mai fidarsi. */
function num(v: unknown): number {
  const n = typeof v === 'string' ? Number.parseFloat(v) : (v as number);
  if (Number.isNaN(n)) throw new Error(`Numero non valido dal database: ${String(v)}`);
  return n;
}

function data(v: unknown): string {
  return String(v).slice(0, 10);
}

export function aIngrediente(r: Record<string, unknown>): Ingredient {
  return {
    id: String(r.id),
    nome: String(r.nome),
    unitaBase: r.unita_base as UnitaBase,
    area: r.area as AreaId,
    classeResiduo: r.classe_residuo as ClasseResiduo,
    deperibile: Boolean(r.deperibile),
    formatoConfezione: num(r.formato_confezione),
  };
}

export function aMealSlot(r: Record<string, unknown>): MealSlot {
  return {
    id: String(r.id),
    data: data(r.data),
    slotDefId: String(r.slot_def_id),
    stato: r.stato as StatoSlot,
    dishId: r.dish_id ? String(r.dish_id) : null,
    fonteStato: r.fonte_stato as FonteStato,
  };
}

export function aPantryState(r: Record<string, unknown>): PantryState {
  return {
    ingredientId: String(r.ingredient_id),
    residuo: num(r.residuo),
    ultimoAcquisto: r.ultimo_acquisto ? data(r.ultimo_acquisto) : null,
    giorniStimati: num(r.giorni_stimati),
    ultimoCheck: r.ultimo_check ? data(r.ultimo_check) : null,
  };
}

export function aSlotDef(r: Record<string, unknown>): MealSlotDef {
  return {
    id: String(r.id),
    nome: String(r.nome),
    posizione: num(r.posizione),
    assenzeAbituali: (r.assenze_abituali as boolean[]).map(Boolean),
  };
}

export function aDishIngredient(r: Record<string, unknown>) {
  return {
    ingredientId: String(r.ingredient_id),
    quantita: num(r.quantita),
    unita: r.unita as UnitaMisura,
  };
}
```

- [ ] **Step 6: Eseguire il test**

Run: `npx vitest run src/data/__tests__/mappers.test.ts`
Expected: PASS, 5 test.

- [ ] **Step 7: Scrivere i moduli di accesso**

Ogni modulo espone funzioni che restituiscono tipi di dominio. Esempio completo, `src/data/repertorio.ts`:

```ts
import type { Dish, Ingredient } from '@/domain/types';
import { client } from './supabase';
import { aDishIngredient, aIngrediente } from './mappers';

export async function leggiIngredienti(): Promise<Ingredient[]> {
  const { data, error } = await client().from('ingredient').select('*').order('nome');
  if (error) throw error;
  return data.map(aIngrediente);
}

export async function leggiRepertorio(): Promise<Dish[]> {
  const { data, error } = await client()
    .from('dish')
    .select('id, nome, slot_def_id, fonte, attivo, dish_ingredient(ingredient_id, quantita, unita)')
    .order('created_at');
  if (error) throw error;
  return data.map((r) => ({
    id: String(r.id),
    nome: String(r.nome),
    slotDefId: String(r.slot_def_id),
    fonte: r.fonte as Dish['fonte'],
    attivo: Boolean(r.attivo),
    ingredienti: (r.dish_ingredient ?? []).map(aDishIngredient),
  }));
}

export async function salvaPiatto(
  piatto: Omit<Dish, 'id'> & { id?: string },
): Promise<string> {
  const sb = client();
  const { data: utente } = await sb.auth.getUser();
  const userId = utente.user!.id;

  const { data: riga, error } = await sb
    .from('dish')
    .upsert({
      id: piatto.id,
      user_id: userId,
      nome: piatto.nome,
      slot_def_id: piatto.slotDefId,
      fonte: piatto.fonte,
      attivo: piatto.attivo,
    })
    .select('id')
    .single();
  if (error) throw error;

  const dishId = String(riga.id);
  // Le righe ingrediente si riscrivono in blocco: sono poche e la diff non vale il codice.
  const { error: eDel } = await sb.from('dish_ingredient').delete().eq('dish_id', dishId);
  if (eDel) throw eDel;
  if (piatto.ingredienti.length > 0) {
    const { error: eIns } = await sb.from('dish_ingredient').insert(
      piatto.ingredienti.map((i) => ({
        user_id: userId,
        dish_id: dishId,
        ingredient_id: i.ingredientId,
        quantita: i.quantita,
        unita: i.unita,
      })),
    );
    if (eIns) throw eIns;
  }
  return dishId;
}

export async function salvaIngrediente(
  ing: Omit<Ingredient, 'id'> & { id?: string },
): Promise<string> {
  const sb = client();
  const { data: utente } = await sb.auth.getUser();
  const { data, error } = await sb
    .from('ingredient')
    .upsert({
      id: ing.id,
      user_id: utente.user!.id,
      nome: ing.nome,
      unita_base: ing.unitaBase,
      area: ing.area,
      classe_residuo: ing.classeResiduo,
      deperibile: ing.deperibile,
      formato_confezione: ing.formatoConfezione,
    })
    .select('id')
    .single();
  if (error) throw error;

  // Ogni ingrediente ha una riga di dispensa dal primo giorno, a residuo zero:
  // "punto di partenza del residuo: zero" della spec.
  await sb.from('pantry_state').upsert(
    { ingredient_id: String(data.id), user_id: utente.user!.id, residuo: 0 },
    { onConflict: 'ingredient_id', ignoreDuplicates: true },
  );
  return String(data.id);
}
```

Gli altri quattro moduli seguono lo stesso schema — `client()`, `select`/`upsert`, `if (error) throw error`, mapper in uscita — con queste firme esatte. **Sono il contratto che i Task 9-16 consumano: non cambiarle.**

```ts
// src/data/settimana.ts
export async function leggiSettimanaCorrente(): Promise<{ id: string; dataInizio: string; stato: 'bozza' | 'confermata' | 'chiusa'; slots: MealSlot[] } | null>;
/** Genera i default con generaSettimana + assegnaPiatti e li scrive. Restituisce il week id. */
export async function creaSettimana(lunedi: string): Promise<string>;
/** Passa sempre per applicaStato: una fonte debole non sovrascrive una forte. */
export async function aggiornaSlot(slotId: string, patch: { stato?: StatoSlot; dishId?: string | null }, fonte: FonteStato): Promise<void>;
export async function confermaSettimana(weekId: string): Promise<void>;
/** Usata da generaListe nel Task 14. */
export async function leggiSlotSettimana(weekId: string): Promise<MealSlot[]>;

// src/data/impostazioni.ts
export async function leggiImpostazioni(): Promise<Impostazioni>;
export async function salvaImpostazioni(i: Impostazioni): Promise<void>;
/** Ordinati per posizione. Da 3 a 5 righe. */
export async function leggiSlotDefs(): Promise<MealSlotDef[]>;
/** Riscrive l'intero insieme; rifiuta meno di 3 o più di 5. */
export async function salvaSlotDefs(defs: MealSlotDef[]): Promise<void>;

// src/data/dispensa.ts
export async function leggiDispensa(): Promise<PantryState[]>;
/** "sì" scrive ultimo_check = oggi; "no" trasforma il controllo in voce d'acquisto. */
export async function rispondiControllo(ingredientId: string, listaId: string, ancora: boolean): Promise<void>;

// src/data/lista.ts
export async function generaListe(weekId: string): Promise<void>;              // corpo nel Task 14
export async function leggiListe(weekId: string): Promise<ListaSalvata | null>;
export async function spunta(itemId: string, spuntato: boolean): Promise<void>;
export async function chiudiSpesa(weekId: string): Promise<void>;              // corpo nel Task 15
```

**`leggiListe` non restituisce `SezioneLista`.** Quel tipo esce da `costruisciLista` e descrive un calcolo, non una riga salvata: non ha l'id che serve alla spunta né lo stato spuntato. Il tipo persistito è un altro, e sta in `src/data/lista.ts` accanto alle sue funzioni:

```ts
export interface VoceSalvata {
  /** id di shopping_list_item: è questo che la coda offline accoda. */
  id: string;
  ingredientId: string;
  nome: string;
  area: AreaId;
  unita: UnitaBase;
  fabbisogno: number;
  residuo: number;
  confezioni: number;
  quantitaTotale: number;
  spuntato: boolean;
  origine: 'piano' | 'controllo' | 'manuale';
  /** true sulle sole voci porzionabili: mostra "serve X · in casa Y". */
  mostraDettaglio: boolean;
}

export interface SezioneSalvata {
  area: AreaId;
  voci: VoceSalvata[];       // origine 'piano' o 'manuale'
  controlli: VoceSalvata[];  // origine 'controllo'
}

export interface ListaSalvata {
  base: SezioneSalvata[];
  topup: SezioneSalvata[];
}
```

`leggiListe` ricostruisce le sezioni **dalle righe congelate in `shopping_list_item`**, non richiamando `costruisciLista`: la lista non deve cambiare sotto gli occhi di chi è in corsia. Fa un join su `ingredient` per prendere `nome`, `deperibile` e `classe_residuo` (da cui deriva `mostraDettaglio === classeResiduo === 'porzionabile'`), raggruppa per `area` seguendo `impostazioni.ordineAree`, e ordina le voci per `confezioni` decrescenti poi per nome — le stesse due regole della funzione `sezioni()` del Task 4.

- [ ] **Step 8: Aggiungere autenticazione con magic link**

`src/app/entra/page.tsx` — una pagina con un solo campo email che chiama `supabase.auth.signInWithOtp({ email })` e mostra "Controlla la posta". Stile: fondo `var(--fondo)`, titolo 52px/800 come le altre schermate, pulsante primario `var(--ink)` con raggio 18px e ombra `0 3px 10px rgba(20,22,58,0.24)`.

`src/middleware.ts` reindirizza a `/entra` chi non ha sessione, lasciando passare `/entra` stessa e gli asset statici.

- [ ] **Step 9: Verificare il giro completo**

Run: `npm run dev`, aprire `/entra`, inserire l'email, seguire il link ricevuto.
Expected: la sessione persiste dopo il reload; una query su `ingredient` da browser autenticato restituisce zero righe senza errore.

- [ ] **Step 10: Commit**

```bash
git add src/data src/app/entra src/middleware.ts package.json package-lock.json
git commit -m "feat(data): client Supabase, mapper tipizzati, moduli di accesso e magic link"
```

---

## Task 8: Shell dell'app — marchio, testata, tab bar

**Files:**
- Create: `src/components/Marchio.tsx`, `src/components/Testata.tsx`, `src/components/TabBar.tsx`
- Create: `src/app/(app)/layout.tsx`
- Test: `src/components/__tests__/Marchio.test.tsx`
- **Artboard di riferimento:** `design/Lista.dc.html` (testata + tab bar), `design/Settimana.dc.html` (marchio tutto pieno)

**Interfaces:**
- Consumes: `AreaId`, `ORDINE_MARCHIO`, `coloreArea` dal Task 2
- Produces: `<Marchio aree={AreaId[]} />` dove `aree` sono le aree **che hanno ancora qualcosa da prendere** (quelle contornate); `<Testata titolo={string} settimana?={string} />`; `<TabBar />`

- [ ] **Step 1: Scrivere il test del marchio (fallirà)**

È la regola con più storia dietro e la più facile da rompere: merita un test.

`src/components/__tests__/Marchio.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { Marchio } from '../Marchio';

function caselle(c: HTMLElement) {
  return Array.from(c.querySelectorAll('[data-area]')) as HTMLElement[];
}

describe('Marchio', () => {
  it('ha sempre sei caselle', () => {
    const { container } = render(<Marchio aree={[]} />);
    expect(caselle(container)).toHaveLength(6);
  });

  it('le dispone nell\'ordine ruotato: arancio, azzurro, verde, lilla, giallo, corallo', () => {
    const { container } = render(<Marchio aree={[]} />);
    expect(caselle(container).map((e) => e.dataset.area)).toEqual([
      'dispensa', 'latticini', 'ortofrutta', 'surgelati', 'cereali', 'macelleria',
    ]);
  });

  it('riempie tutto quando non manca niente', () => {
    const { container } = render(<Marchio aree={[]} />);
    expect(caselle(container).every((e) => e.dataset.stato === 'pieno')).toBe(true);
  });

  it('contorna solo le aree in cui manca qualcosa', () => {
    const { container } = render(<Marchio aree={['latticini', 'cereali']} />);
    const vuote = caselle(container).filter((e) => e.dataset.stato === 'vuoto');
    expect(vuote.map((e) => e.dataset.area).sort()).toEqual(['cereali', 'latticini']);
  });

  it('non usa mai il grigio: il bordo è sempre il colore dell\'area', () => {
    const { container } = render(<Marchio aree={['latticini']} />);
    const latticini = caselle(container).find((e) => e.dataset.area === 'latticini')!;
    expect(latticini.style.borderColor).toBe('rgb(156, 199, 242)'); // #9CC7F2
    expect(latticini.style.background).toBe('transparent');
  });
});
```

- [ ] **Step 2: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/components/__tests__/Marchio.test.tsx`
Expected: FAIL — modulo non risolto.

- [ ] **Step 3: Implementare il marchio**

`src/components/Marchio.tsx`:

```tsx
import type { AreaId } from '@/domain/types';
import { ORDINE_MARCHIO, coloreArea } from '@/domain/aree';

interface Props {
  /** Le aree in cui manca ancora qualcosa. Vuoto = marchio tutto pieno. */
  aree: AreaId[];
  lato?: number;
}

/**
 * Griglia 3×2 nell'ordine fisso del marchio. Ogni casella è sempre nel colore
 * della sua area: piena quando in quell'area non manca niente — o l'hai
 * completata, o non era in questa spesa — contornata quando manca qualcosa.
 * Mai grigia.
 */
export function Marchio({ aree, lato = 16 }: Props) {
  const manca = new Set(aree);
  return (
    <div style={{ display: 'grid', gridTemplateColumns: `repeat(3, ${lato}px)`, gap: 4, flex: 'none' }}>
      {ORDINE_MARCHIO.map((area) => {
        const vuoto = manca.has(area);
        const colore = coloreArea(area);
        return (
          <div
            key={area}
            data-area={area}
            data-stato={vuoto ? 'vuoto' : 'pieno'}
            style={{
              width: lato, height: lato, borderRadius: lato * 0.28, boxSizing: 'border-box',
              border: `2px solid ${colore}`,
              background: vuoto ? 'transparent' : colore,
            }}
          />
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: Eseguire il test**

Run: `npx vitest run src/components/__tests__/Marchio.test.tsx`
Expected: PASS, 5 test.

- [ ] **Step 5: Implementare testata e tab bar**

`Testata.tsx`: marchio + titolo 52px/800/`-0.05em`, e a destra il burger 44×44 che porta a `/impostazioni`. Se `settimana` è passata, sotto compare la pillola scura `#14163A` alta 34px con l'etichetta mono e la freccetta — **inerte, senza onClick** (debito dichiarato nella spec).

`TabBar.tsx`: tre voci LISTA / SETTIMANA / PIATTI. Attiva = fondo `rgba(20,22,58,0.06)` con raggio 14px, icona e testo `#14163A` peso 700. Inattiva = `#9A9AA6` peso 500. Le tre icone SVG si copiano da `design/Settimana.dc.html`, che le contiene tutte e tre nella versione definitiva (nota: l'icona Piatti è la forchetta-e-coltello di `Settimana.dc.html`, non il libro di `Lista.dc.html`, che è la versione vecchia).

`src/app/(app)/layout.tsx`: colonna flex a tutta altezza, `<main>` scrollabile con `flex: 1`, `<TabBar />` in fondo.

- [ ] **Step 6: Verificare a occhio**

Run: `npm run dev`, aprire `/lista` (per ora vuota) su viewport 393×852.
Expected: marchio tutto pieno accanto al titolo, tab bar in fondo con LISTA attiva. Confrontare con `design/Lista.dc.html` aperto nel canvas.

- [ ] **Step 7: Commit**

```bash
git add src/components src/app
git commit -m "feat(ui): marchio, testata e tab bar"
```

---

## Task 9: Piatti — il repertorio

**Files:**
- Create: `src/app/(app)/piatti/page.tsx`
- Create: `src/components/Segmento.tsx`
- **Artboard:** `design/Piatti.dc.html`, stato vuoto `design/VuotoPiatti.dc.html`

**Interfaces:**
- Consumes: `leggiRepertorio`, `leggiIngredienti`, `leggiSlotDefs` dal Task 7; `coloreArea` dal Task 2
- Produces: `<Segmento opzioni={{id, label}[]} valore={string} onCambia={(id) => void} />`, riusato da Piatto, Ingrediente e Lista

- [ ] **Step 1: Costruire il segmento a pillole**

`Segmento.tsx`: riga scrollabile orizzontalmente di pillole 999px alte 38-40px. Attiva `#14163A` su testo bianco peso 700; inattiva bianca con bordo `rgba(20,22,58,0.09)` e testo `#8A8A96` peso 500. Font mono 10.5px, letter-spacing `0.09em`.

- [ ] **Step 2: Costruire la lista dei piatti**

Ogni piatto è una scheda bianca raggio 20px, bordo `var(--bordo)`:
- nome 17px/700/`-0.03em`, troncato con ellissi su una riga
- chip dello slot: mono 8.5px/700, bordo `rgba(20,22,58,0.18)`, raggio 999px
- meta mono: `N INGR. · NUTRIZIONISTA` oppure `· PROPRIO`
- i pallini delle aree: quadratini 8×8 raggio 2.6px, **uno per area distinta** presente negli ingredienti del piatto, nell'ordine delle aree impostato dall'utente

I filtri in cima sono `TUTTI` più un'opzione per ogni `meal_slot_def` dell'utente — **non l'elenco fisso a quattro** dell'artboard, che è mock. Tap sulla scheda → `/piatti/[id]`.

- [ ] **Step 3: Costruire lo stato vuoto**

Se `leggiRepertorio()` torna vuoto, rendere `VuotoPiatti`: è l'onboarding. Copy alla lettera da `design/VuotoPiatti.dc.html` — titolo «Non hai ancora nessun piatto», i tre passi (Crea i tuoi piatti / Dì dove sarai / Ricevi la lista), la riga sul costo iniziale, e il pulsante `CREA IL PRIMO PIATTO` → `/piatti/nuovo`.

- [ ] **Step 4: Verificare i due stati**

Run: `npm run dev`, aprire `/piatti` con database vuoto, poi inserire due piatti a mano dall'SQL Editor e ricaricare.
Expected: prima l'onboarding, poi le due schede con i filtri corretti.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/piatti src/components/Segmento.tsx
git commit -m "feat(ui): repertorio dei piatti con filtro per pasto e onboarding"
```

---

## Task 10: Piatto — editor della ricetta

**Files:**
- Create: `src/app/(app)/piatti/[id]/page.tsx`
- Create: `src/components/TesseraIngrediente.tsx`
- **Artboard:** `design/Piatto.dc.html`, stato nuovo `design/VuotoPiatto.dc.html`

**Interfaces:**
- Consumes: `salvaPiatto`, `leggiIngredienti`, `leggiSlotDefs`, `leggiSettimanaCorrente` dal Task 7
- Produces: la rotta `/piatti/[id]`, con `id === 'nuovo'` per la creazione

- [ ] **Step 1: Costruire la griglia degli ingredienti**

Griglia a due colonne di tessere raggio 15px, alte almeno 108px, fondo bianco con bordo nel colore dell'area al 45% e ombra `0 1px 2px rgba(20,22,58,0.05)`. Dentro ogni tessera, dall'alto: la pillola della grammatura (mono 10.5px/700 su fondo colore-area al 32%), il nome 17px/700, e in fondo il nome dell'area in mono 8px `#A6A6B2`.

**La grammatura è la porzione del piano per una persona** — mai moltiplicata qui: il moltiplicatore vive nelle Impostazioni e agisce solo in `list-builder`.

- [ ] **Step 2: Aggiungere la tessera "aggiungi"**

Stessa dimensione delle altre, fondo trasparente, bordo 1.5px tratteggiato `rgba(20,22,58,0.20)`, un `+` al centro. Tap → selettore ingrediente esistente o `/piatti/[id]/ingredienti/nuovo`.

- [ ] **Step 3: Aggiungere nome, slot e riepilogo settimana**

Nome del piatto come campo di testo che eredita lo stile del titolo di dettaglio (32-34px/800/`-0.045em`), placeholder «Dai un nome al piatto». Sotto, il `<Segmento>` degli slot dai `meal_slot_def` reali.

In fondo, la striscia dei sette giorni: quelli in cui il piatto è in programma questa settimana sono `#14163A` su testo bianco, gli altri `rgba(20,22,58,0.045)` su `#A6A6B2`. Sotto, la frase di riepilogo. Se il piatto non è in programma, il testo è quello di `VuotoPiatto`: «Non ancora in programma. Comparirà qui appena lo assegni a un pasto dalla Settimana.»

- [ ] **Step 4: Bloccare il salvataggio di un piatto senza ingredienti**

Un piatto senza ingredienti non entra mai nella lista, quindi salvarlo è un errore silenzioso. Con zero ingredienti, il pulsante `SALVA PIATTO` è disattivato (`rgba(20,22,58,0.10)` su `#A6A6B2`) e sopra compare la spiegazione di `VuotoPiatto`: «Un piatto senza ingredienti non entra nella lista della spesa: è la grammatura di ogni ingrediente a dire quanto comprare. Aggiungine almeno uno.»

- [ ] **Step 5: Verificare il giro**

Run: `npm run dev`. Creare un piatto da `/piatti/nuovo`, aggiungere due ingredienti, salvare, riaprirlo.
Expected: i valori tornano identici; con zero ingredienti il salvataggio resta bloccato.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(app\)/piatti src/components/TesseraIngrediente.tsx
git commit -m "feat(ui): editor del piatto a tessere, con blocco sul piatto senza ingredienti"
```

---

## Task 11: Ingrediente

**Files:**
- Create: `src/app/(app)/piatti/[id]/ingredienti/[ingId]/page.tsx`
- **Artboard:** `design/Ingrediente.dc.html`

**Interfaces:**
- Consumes: `salvaIngrediente`, `leggiIngredienti` dal Task 7; `AREE` dal Task 2
- Produces: la rotta dell'ingrediente

- [ ] **Step 1: Costruire il selettore d'area**

Griglia 2×3 delle sei aree. Selezionata: fondo colore-area al 22%, bordo 1.5px pieno nel colore. Non selezionata: bianca con bordo `rgba(20,22,58,0.09)`, testo `#8A8A96`. Ogni cella ha il pallino 11×11 nel colore dell'area.

- [ ] **Step 2: Costruire unità, formato confezione e classe**

Tre segmenti alti 46px raggio 14px: unità `G` / `ML` / `PZ`; classe `PORZIONABILE` / `INTERO` / `A STIMA`. In mezzo, il campo numerico del formato confezione, con l'unità scelta come suffisso.

Sotto il segmento della classe, la spiegazione cambia con la scelta — testo alla lettera da `Ingrediente.dc.html`:

```ts
const SPIEGA = {
  porzionabile: 'La confezione copre più pasti. L’app calcola quanto ne resta dopo ogni porzione e lo riporta alla settimana dopo.',
  intero: 'Si conta a pezzi e non lascia resti frazionari: sei uova sono sei uova.',
  stima: 'Non vale la pena contarlo a grammi. Ogni 90 giorni dall’ultimo acquisto la lista ti chiede se ne hai ancora.',
};
```

Scegliendo `INTERO`, l'unità passa automaticamente a `PZ` e il formato confezione si blocca a 1: è quello che fa `list-builder`, e mostrare altro mentirebbe.

- [ ] **Step 3: Costruire l'interruttore deperibile**

Track 52×31 raggio 999px, knob bianco 25×25 con ombra `0 1px 3px rgba(20,22,58,0.28)`. Acceso `#14163A`, spento `rgba(20,22,58,0.14)`. Sotto, l'etichetta che dice dove finirà: `FINISCE NELLA LISTA TOP-UP` oppure `FINISCE NELLA LISTA BASE`. È l'unico posto in cui l'utente vede lo split, e va detto qui.

- [ ] **Step 4: Verificare**

Run: `npm run dev`. Creare un ingrediente porzionabile deperibile e uno intero.
Expected: scegliendo `INTERO` l'unità diventa `PZ` e il formato si blocca a 1; l'etichetta base/top-up segue l'interruttore.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/piatti
git commit -m "feat(ui): scheda ingrediente con area, classe di residuo e deperibilità"
```

---

## Task 12: Settimana — il piano alimentare

**Files:**
- Create: `src/app/(app)/settimana/page.tsx`
- Create: `src/components/StrisciaGiorni.tsx`, `src/components/RigaPasto.tsx`
- **Artboard:** `design/Settimana.dc.html`, stato `design/VuotoLista.dc.html` per il rimando

**Interfaces:**
- Consumes: `generaSettimana`, `assegnaPiatti`, `applicaStato` dal Task 5; `leggiSettimanaCorrente`, `creaSettimana`, `aggiornaSlot`, `confermaSettimana` dal Task 7
- Produces: la rotta `/settimana`; il pulsante che porta al Task 14 via `generaListe`

- [ ] **Step 1: Costruire la striscia dei sette giorni**

Sette riquadri in fila, raggio 14px. Selezionato: fondo `#14163A`, testo bianco, ombra `0 2px 6px rgba(20,22,58,0.20)`. Non selezionato: bianco con bordo 1px `rgba(20,22,58,0.07)`.

**Il giorno corrente ha `border: 3px solid #14163A` sul riquadro stesso** — non un `outline` esterno, non un cerchio intorno. Vale anche quando non è il giorno selezionato.

Sotto il numero, un pallino da 5px per ogni pasto: pieno se quel pasto è a casa e ha un piatto, scarico altrimenti. **Il numero di pallini è il numero di `meal_slot_def`, da 3 a 5.**

- [ ] **Step 2: Costruire le righe dei pasti del giorno**

Una riga per pasto. La riga ha tre zone e due comportamenti distinti — è il punto in cui il design è stato corretto due volte, quindi va rispettato alla lettera:

| Zona | Larghezza | Tap |
|---|---|---|
| Casa a sinistra | 60px | **Accende/spegne** lo stato |
| Corpo centrale | flessibile | **Apre** il dettaglio del piatto |
| Freccia a destra | 44px | Apre `Scegli il piatto` per quel pasto |

Stato a casa: casa piena `#14163A`, scheda bianca con bordo `rgba(20,22,58,0.09)`, nome del piatto 17.5px/700, sotto i quadratini delle aree.
Stato fuori: casa **bianca con contorno `#BFBFC9` a 1.3px** (senza il contorno sarebbe invisibile sul fondo chiaro), scheda `rgba(20,22,58,0.04)`, testo «Fuori casa» barrato in `#B6B6C0`.

Ogni cambio di stato chiama `applicaStato(slot, nuovoStato, 'checkin')`.

- [ ] **Step 3: Creare la settimana al primo accesso**

Se non esiste una `week` per il lunedì corrente: `creaSettimana` chiama `generaSettimana` con gli slot def e le loro assenze abituali, poi `assegnaPiatti`, poi salva. L'utente trova la settimana già compilata e corregge — è il principio "default automatico + correzione facile".

- [ ] **Step 4: Aggiungere conferma e contatore**

In fondo: a sinistra `N PASTI A CASA IN SETTIMANA` in mono 10px/700, a destra la legenda. Sotto, il pulsante `CONFERMA E CREA LA LISTA`, alto 54px raggio 18px, `#14163A` con ombra `0 3px 10px rgba(20,22,58,0.24)`.

Il tap chiama `confermaSettimana` e poi `generaListe` (Task 14), quindi naviga a `/lista`.

- [ ] **Step 5: Verificare**

Run: `npm run dev`, aprire `/settimana`.
Expected: la settimana si genera con i pranzi infrasettimanali già spenti; il tap sulla casa cambia stato, il tap sul corpo apre il piatto; oggi ha il bordo 3px anche selezionando un altro giorno.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(app\)/settimana src/components
git commit -m "feat(ui): settimana come piano alimentare, con check-in a un tap"
```

---

## Task 13: Scegli il piatto

**Files:**
- Create: `src/app/(app)/settimana/[data]/[slotDefId]/scegli/page.tsx`
- **Artboard:** `design/Scegli.dc.html`

**Interfaces:**
- Consumes: `leggiRepertorio`, `aggiornaSlot` dal Task 7
- Produces: la rotta di sostituzione

- [ ] **Step 1: Costruire l'elenco**

Mostra **solo i piatti attivi di quello slot**. Ogni riga: scheda bianca raggio 20px con nome, meta `N INGR.`, quadratini delle aree, e a destra un cerchio 24px — pieno `#14163A` se selezionato, altrimenti bordo 1.5px `rgba(20,22,58,0.20)`. La scheda selezionata ha bordo 1.5px `#14163A`.

- [ ] **Step 2: Aggiungere la nota che spiega la portata**

Sotto l'elenco, il testo cambia con la scelta — alla lettera dall'artboard, con il pasto e il giorno reali al posto di «la cena di giovedì»:

- niente cambiato: «Tocca un piatto per sostituire *la cena di giovedì*. Vale solo per quel giorno, non cambia il piatto nel repertorio.»
- cambiato: «Cambia solo *la cena di giovedì*. Gli altri giorni restano come sono, e la lista della spesa si ricalcola da sola.»

Questa schermata **non tocca mai il repertorio**: scrive solo `meal_slot.dish_id` di quel singolo slot.

- [ ] **Step 3: Aggiungere il pulsante di conferma**

Disattivato (`rgba(20,22,58,0.10)` su `#A6A6B2`) finché non cambia niente. Attivo, chiama `aggiornaSlot` e torna alla Settimana.

**Debito noto, dichiarato nella spec:** in Fase 1 la sostituzione non avvisa se il nuovo piatto crea un conflitto in dispensa («se usi lo yogurt qui non ti resta per giovedì»). Richiede la simulazione del residuo in avanti ed è lavoro di Fase 3. Non aggiungerlo qui.

- [ ] **Step 4: Verificare**

Run: `npm run dev`. Dalla Settimana, freccia su un pasto, scegliere un altro piatto, confermare.
Expected: cambia solo quel giorno; gli altri giorni con lo stesso piatto restano invariati.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/settimana
git commit -m "feat(ui): sostituzione del piatto per singolo pasto"
```

---

## Task 14: Lista — generazione, tessere e spunta offline

**Files:**
- Create: `src/app/(app)/lista/page.tsx`
- Create: `src/components/Tessera.tsx`, `src/components/RigaControllo.tsx`
- Create: `src/offline/coda.ts`
- Modify: `src/data/lista.ts`
- Test: `src/offline/__tests__/coda.test.ts`
- **Artboard:** `design/Lista.dc.html`, stato `design/VuotoLista.dc.html`

**Interfaces:**
- Consumes: `costruisciLista` e `ListaRisultato` dal Task 4 — **solo dentro `generaListe`**, mai dalla pagina; `ListaSalvata`, `SezioneSalvata`, `VoceSalvata` dal Task 7, che è ciò che la pagina rende; `<Marchio>` dal Task 8
- Produces: il corpo di `generaListe(weekId)`, e la coda offline `accodaSpunta`, `leggiCoda`, `svuotaCoda`, `applicaCodaSuVoci`

- [ ] **Step 1: Scrivere il test della coda offline (fallirà)**

`src/offline/__tests__/coda.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { accodaSpunta, leggiCoda, svuotaCoda, applicaCodaSuVoci } from '../coda';

beforeEach(() => localStorage.clear());

describe('coda delle spunte', () => {
  it('accoda una spunta', () => {
    accodaSpunta('v1', true, 1000);
    expect(leggiCoda()).toEqual([{ itemId: 'v1', spuntato: true, ts: 1000 }]);
  });

  it('tiene solo l\'ultima decisione per voce: la spunta è idempotente', () => {
    accodaSpunta('v1', true, 1000);
    accodaSpunta('v1', false, 2000);
    expect(leggiCoda()).toEqual([{ itemId: 'v1', spuntato: false, ts: 2000 }]);
  });

  it('non lascia che un evento vecchio sovrascriva uno nuovo', () => {
    accodaSpunta('v1', false, 2000);
    accodaSpunta('v1', true, 1000);
    expect(leggiCoda()).toEqual([{ itemId: 'v1', spuntato: false, ts: 2000 }]);
  });

  it('tiene separate voci diverse', () => {
    accodaSpunta('v1', true, 1000);
    accodaSpunta('v2', true, 1001);
    expect(leggiCoda()).toHaveLength(2);
  });

  it('sovrascrive lo stato del server con quello locale in attesa', () => {
    accodaSpunta('v1', true, 1000);
    const voci = [{ id: 'v1', spuntato: false }, { id: 'v2', spuntato: true }];
    expect(applicaCodaSuVoci(voci)).toEqual([
      { id: 'v1', spuntato: true }, { id: 'v2', spuntato: true },
    ]);
  });

  it('si svuota', () => {
    accodaSpunta('v1', true, 1000);
    svuotaCoda();
    expect(leggiCoda()).toEqual([]);
  });

  it('sopravvive a un localStorage corrotto invece di rompere la schermata', () => {
    localStorage.setItem('spesa:coda', 'non è json');
    expect(leggiCoda()).toEqual([]);
  });
});
```

- [ ] **Step 2: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/offline/__tests__/coda.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 3: Implementare la coda**

`src/offline/coda.ts`:

```ts
const CHIAVE = 'spesa:coda';

export interface Spunta {
  itemId: string;
  spuntato: boolean;
  /** Millisecondi. Decide chi vince fra due eventi sulla stessa voce. */
  ts: number;
}

export function leggiCoda(): Spunta[] {
  if (typeof localStorage === 'undefined') return [];
  try {
    const grezzo = localStorage.getItem(CHIAVE);
    if (!grezzo) return [];
    const v = JSON.parse(grezzo);
    return Array.isArray(v) ? (v as Spunta[]) : [];
  } catch {
    // Meglio perdere la coda che lasciare l'utente davanti a una schermata rotta
    // mentre è in corsia.
    return [];
  }
}

function scrivi(coda: Spunta[]) {
  localStorage.setItem(CHIAVE, JSON.stringify(coda));
}

/**
 * Una sola voce in coda per itemId, quella più recente: la sincronizzazione
 * diventa idempotente e riprodurre la coda due volte non cambia il risultato.
 */
export function accodaSpunta(itemId: string, spuntato: boolean, ts = Date.now()): void {
  const coda = leggiCoda();
  const i = coda.findIndex((s) => s.itemId === itemId);
  if (i === -1) {
    coda.push({ itemId, spuntato, ts });
  } else if (ts >= coda[i].ts) {
    coda[i] = { itemId, spuntato, ts };
  }
  scrivi(coda);
}

export function svuotaCoda(): void {
  localStorage.removeItem(CHIAVE);
}

/** Lo stato locale in attesa ha sempre ragione su quello letto dal server. */
export function applicaCodaSuVoci<T extends { id: string; spuntato: boolean }>(voci: T[]): T[] {
  const coda = new Map(leggiCoda().map((s) => [s.itemId, s.spuntato]));
  return voci.map((v) => (coda.has(v.id) ? { ...v, spuntato: coda.get(v.id)! } : v));
}
```

- [ ] **Step 4: Eseguire il test**

Run: `npx vitest run src/offline/__tests__/coda.test.ts`
Expected: PASS, 7 test.

- [ ] **Step 5: Implementare la generazione delle liste**

In `src/data/lista.ts`, `generaListe(weekId)` legge slot, piatti, ingredienti, dispensa e impostazioni, chiama `costruisciLista`, e **congela il risultato** in `shopping_list` + `shopping_list_item`. Le quantità non si ricalcolano al volo a ogni apertura: chi è in corsia non deve vedere la lista cambiare sotto gli occhi perché nel frattempo ha spuntato qualcosa.

```ts
export async function generaListe(weekId: string): Promise<void> {
  const sb = client();
  const { data: u } = await sb.auth.getUser();
  const userId = u.user!.id;

  const [slots, dishes, ingredients, pantry, impostazioni] = await Promise.all([
    leggiSlotSettimana(weekId), leggiRepertorio(), leggiIngredienti(),
    leggiDispensa(), leggiImpostazioni(),
  ]);

  const risultato = costruisciLista({
    slots, dishes, ingredients, pantry, impostazioni,
    oggi: new Date().toISOString().slice(0, 10),
  });

  for (const tipo of ['base', 'topup'] as const) {
    const { data: lista, error } = await sb.from('shopping_list')
      .upsert({ user_id: userId, week_id: weekId, tipo }, { onConflict: 'week_id,tipo' })
      .select('id').single();
    if (error) throw error;

    await sb.from('shopping_list_item').delete().eq('shopping_list_id', lista.id);

    const sezioni = risultato[tipo];
    const righe = [
      ...sezioni.flatMap((s) => s.voci.map((v) => ({
        user_id: userId, shopping_list_id: lista.id, ingredient_id: v.ingredientId,
        fabbisogno: v.fabbisogno, residuo: v.residuo, confezioni: v.confezioni,
        quantita_totale: v.quantitaTotale, unita: v.unita, area: v.area, origine: 'piano',
      }))),
      ...sezioni.flatMap((s) => s.controlli.map((c) => ({
        user_id: userId, shopping_list_id: lista.id, ingredient_id: c.ingredientId,
        fabbisogno: 0, residuo: 0, confezioni: 0,
        quantita_totale: 0, unita: c.unita, area: c.area, origine: 'controllo',
      }))),
    ];
    if (righe.length > 0) {
      const { error: e } = await sb.from('shopping_list_item').insert(righe);
      if (e) throw e;
    }
  }
}
```

- [ ] **Step 6: Costruire le tessere**

Griglia a due colonne dentro una scheda bianca per area. La **prima voce di ogni area** occupa due colonne, ha fondo pieno nel colore dell'area e nome a 25px. Le altre stanno su una colonna, fondo bianco, bordo nel colore al 45%, nome 17px.

Accesa = da prendere. Spenta = presa: fondo `rgba(20,22,58,0.035)`, bordo trasparente, nome barrato in `rgba(20,22,58,0.34)`. **Il tap sulla tessera è l'interruttore, non c'è nessuna checkbox.**

La pillola delle confezioni sta in cima alla tessera (`order: -1`), mono 10.5px/700: bianca sulla protagonista, colore-area al 32% sulle altre. Accanto, la quantità totale in mono 10px.

Il sottotitolo `serve 750 g · in casa 50 g` compare solo se `voce.mostraDettaglio` è true.

- [ ] **Step 7: Collegare la spunta alla coda**

Il tap aggiorna subito lo stato in React, chiama `accodaSpunta`, e prova a scrivere sul server. Se la scrittura fallisce o il browser è offline, la voce resta in coda. Su `window.addEventListener('online')` e al montaggio, svuotare la coda scrivendo tutto e poi `svuotaCoda()`. La lista letta dal server passa sempre per `applicaCodaSuVoci`.

- [ ] **Step 8: Costruire le righe di controllo**

Sotto le tessere della sua area, una riga su fondo colore-area al 26%, raggio 18px: nome `Olio: ne hai ancora?`, sotto la nota mono `CONTROLLO OGNI 90 GIORNI · SCADUTO`, e a destra i due pulsanti tondi `SÌ` / `NO` alti 44px.

`SÌ` → `ultimo_check = oggi`, la riga sparisce.
`NO` → la riga diventa una voce d'acquisto normale di una confezione, con `origine = 'controllo'`.

- [ ] **Step 9: Collegare marchio e tab base/top-up**

Il marchio in testata riceve **le aree che hanno ancora voci non spuntate**, considerando entrambe le liste. Un'area assente dalla spesa non compare fra quelle: resta piena.

In fondo, i due tab: quello attivo si allarga con fondo `#14163A` ed etichetta `N DA PRENDERE`; quello inattivo è largo 96px, fondo `rgba(20,22,58,0.05)`, e mostra solo il conteggio.

Se non esiste una lista per la settimana corrente, rendere `VuotoLista`: «La lista non c'è ancora» con il pulsante `VAI ALLA SETTIMANA`.

- [ ] **Step 10: Verificare offline davvero**

Run: `npm run dev`, aprire `/lista`, aprire i DevTools e passare a Offline, spuntare tre voci, tornare online.
Expected: le spunte restano visibili offline; tornando online arrivano al server e `localStorage['spesa:coda']` si svuota. Ricaricando, lo stato è quello atteso.

- [ ] **Step 11: Commit**

```bash
git add src/app/\(app\)/lista src/components src/offline src/data/lista.ts
git commit -m "feat(ui): lista della spesa con tessere, controlli staple e spunta offline"
```

---

## Task 15: Chiudi la spesa — residuo e storico acquisti

**Files:**
- Modify: `src/data/lista.ts`, `src/data/dispensa.ts`
- Create: `src/app/(app)/lista/fatta/page.tsx`
- Test: `src/domain/__tests__/chiusura.test.ts`
- Create: `src/domain/chiusura.ts`
- **Artboard:** `design/VuotoFatta.dc.html`

**Interfaces:**
- Consumes: `nuovoResiduo` dal Task 3
- Produces: `calcolaChiusura(input): AggiornamentoDispensa[]` (puro), `chiudiSpesa(weekId)` (data layer)

- [ ] **Step 1: Scrivere il test della chiusura (fallirà)**

`src/domain/__tests__/chiusura.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { calcolaChiusura } from '../chiusura';

const OGGI = '2026-09-06';

describe('calcolaChiusura', () => {
  it('somma il comprato e sottrae il fabbisogno del piano', () => {
    const r = calcolaChiusura({
      voci: [{ ingredientId: 'yogurt', spuntato: true, quantitaTotale: 1000, fabbisogno: 750, residuo: 50, confezioni: 2, origine: 'piano' }],
      oggi: OGGI,
    });
    expect(r).toEqual([{
      ingredientId: 'yogurt', residuo: 300, ultimoAcquisto: OGGI,
      confezioni: 2, quantita: 1000, registraAcquisto: true,
    }]);
  });

  it('non conta come comprato ciò che non è stato spuntato, ma consuma comunque il piano', () => {
    const r = calcolaChiusura({
      voci: [{ ingredientId: 'yogurt', spuntato: false, quantitaTotale: 1000, fabbisogno: 750, residuo: 50, confezioni: 2, origine: 'piano' }],
      oggi: OGGI,
    });
    expect(r[0].residuo).toBe(0);              // max(0, 50 + 0 − 750)
    expect(r[0].registraAcquisto).toBe(false);
    expect(r[0].ultimoAcquisto).toBeNull();
  });

  it('registra l\'acquisto di una riga di controllo senza toccarne il residuo', () => {
    const r = calcolaChiusura({
      voci: [{ ingredientId: 'olio', spuntato: true, quantitaTotale: 1000, fabbisogno: 0, residuo: 0, confezioni: 1, origine: 'controllo' }],
      oggi: OGGI,
    });
    expect(r[0].registraAcquisto).toBe(true);
    expect(r[0].ultimoAcquisto).toBe(OGGI);
    expect(r[0].residuo).toBeNull();  // la classe stima non tiene residuo
  });

  it('non produce mai un residuo negativo', () => {
    const r = calcolaChiusura({
      voci: [{ ingredientId: 'x', spuntato: false, quantitaTotale: 0, fabbisogno: 900, residuo: 100, confezioni: 0, origine: 'piano' }],
      oggi: OGGI,
    });
    expect(r[0].residuo).toBe(0);
  });
});
```

- [ ] **Step 2: Eseguire il test per vederlo fallire**

Run: `npx vitest run src/domain/__tests__/chiusura.test.ts`
Expected: FAIL — modulo non risolto.

- [ ] **Step 3: Implementare**

`src/domain/chiusura.ts`:

```ts
import { nuovoResiduo } from './pantry';

export interface VoceChiusura {
  ingredientId: string;
  spuntato: boolean;
  quantitaTotale: number;
  fabbisogno: number;
  residuo: number;
  confezioni: number;
  origine: 'piano' | 'controllo' | 'manuale';
}

export interface AggiornamentoDispensa {
  ingredientId: string;
  /** null per la classe stima, che non tiene residuo. */
  residuo: number | null;
  ultimoAcquisto: string | null;
  confezioni: number;
  quantita: number;
  registraAcquisto: boolean;
}

/**
 * Chiudere la spesa è il momento in cui il residuo diventa reale: quello che
 * la lista aveva solo previsto. Una voce non spuntata non è stata comprata,
 * ma il piano la consuma lo stesso — e la settimana dopo se ne ricompra di più.
 */
export function calcolaChiusura(
  input: { voci: VoceChiusura[]; oggi: string },
): AggiornamentoDispensa[] {
  return input.voci.map((v) => {
    const comprato = v.spuntato;
    const daControllo = v.origine === 'controllo';
    return {
      ingredientId: v.ingredientId,
      residuo: daControllo
        ? null
        : nuovoResiduo({
            residuoPrecedente: v.residuo,
            acquistato: comprato ? v.quantitaTotale : 0,
            consumatoDaPiano: v.fabbisogno,
          }),
      ultimoAcquisto: comprato ? input.oggi : null,
      confezioni: comprato ? v.confezioni : 0,
      quantita: comprato ? v.quantitaTotale : 0,
      registraAcquisto: comprato,
    };
  });
}
```

- [ ] **Step 4: Eseguire il test**

Run: `npx vitest run src/domain/__tests__/chiusura.test.ts`
Expected: PASS, 4 test.

- [ ] **Step 5: Collegare al data layer**

`chiudiSpesa(weekId)` legge tutte le voci delle due liste, chiama `calcolaChiusura`, e in una sola andata: aggiorna `pantry_state` (`residuo` dove non è null, `ultimo_acquisto` dove c'è), inserisce le righe in `purchase` per ogni `registraAcquisto`, e scrive `chiusa_il` su entrambe le `shopping_list` e `stato = 'chiusa'` sulla `week`.

- [ ] **Step 6: Costruire la schermata "hai preso tutto"**

Quando tutte le voci di entrambe le liste sono spuntate, `/lista` rende `VuotoFatta`. Copy alla lettera dall'artboard, con i numeri reali: «Hai preso tutto», «*Quattordici* voci su *quattordici*, *sei* aree finite. Il marchio in alto è tutto pieno: ogni area è a posto, non ti manca niente.»

Poi il riquadro `CHIUDENDO LA SPESA`: «L'app registra cosa hai comprato e quando. Serve solo a ricordarti fra 90 giorni che l'olio sta per finire: non lo vedi da nessuna parte finché non serve.»

Due pulsanti: `CHIUDI LA SPESA` (primario) e `TORNA ALLA LISTA` (secondario).

**È l'unico momento in cui il residuo viene scritto.** Senza questo tap la registrazione silenziosa non ha un istante in cui avvenire.

- [ ] **Step 7: Verificare il ciclo completo su due settimane**

Run: `npm run dev`. Settimana 1: confermare, generare, spuntare tutto, chiudere. Poi creare la settimana 2 con lo stesso piano e rigenerare.
Expected: nella settimana 2 le voci porzionabili mostrano un residuo maggiore di zero e alcune sono sparite dalla lista perché coperte. **È il comportamento che giustifica l'intero progetto: verificarlo prima di dire che la Fase 1 è finita.**

- [ ] **Step 8: Commit**

```bash
git add src/domain/chiusura.ts src/domain/__tests__/chiusura.test.ts src/data src/app/\(app\)/lista
git commit -m "feat: chiusura della spesa, residuo reale e storico acquisti"
```

---

## Task 16: Impostazioni e ordine dei reparti

**Files:**
- Create: `src/app/(app)/impostazioni/page.tsx`
- Create: `src/app/(app)/impostazioni/reparti/page.tsx`
- **Artboard:** `design/Impostazioni.dc.html`, `design/Reparti.dc.html`

**Interfaces:**
- Consumes: `leggiImpostazioni`, `salvaImpostazioni`, `leggiSlotDefs`, `salvaSlotDefs` dal Task 7
- Produces: le due rotte

- [ ] **Step 1: Costruire il selettore delle porzioni**

Riga con `−` e `+` e il numero al centro. Da 1 a 6. Sotto, la spiegazione che serve solo a cucinare per più persone e non cambia le grammature dei piatti.

- [ ] **Step 2: Costruire l'editor dei pasti**

Una riga per pasto, con il nome modificabile e le frecce su/giù per riordinare. Sotto ogni pasto, sette pastiglie `L M M G V S D` alte 36px raggio 11px: accesa `#14163A` su bianco significa **abitualmente fuori casa** in quel giorno.

Vincoli: minimo 3 pasti, massimo 5. Sotto il minimo, il pulsante di rimozione è disattivato; al massimo, lo è quello di aggiunta. Il conteggio è mostrato: `N PASTI`.

Cambiare le assenze abituali **non tocca le settimane già create**: agisce sul default di quelle future. Dirlo in una riga sotto la griglia, altrimenti l'utente si aspetta che la settimana in corso cambi.

- [ ] **Step 3: Costruire l'ordine dei reparti**

Sei righe bianche raggio 16px, ognuna con il pallino nel colore, il nome, il numero d'ordine, e le frecce su/giù. Le frecce ai limiti sono al 35% di opacità.

In cima, l'anteprima: sei barrette 26×11 nei colori, nell'ordine corrente. È l'unica personalizzazione delle aree — nomi e insieme sono fissi.

- [ ] **Step 4: Verificare l'effetto**

Run: `npm run dev`. Cambiare l'ordine mettendo i surgelati per primi, tornare a `/lista`.
Expected: le sezioni della lista seguono il nuovo ordine. **Il marchio non cambia** — usa `ORDINE_MARCHIO`, che è fisso.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/impostazioni
git commit -m "feat(ui): impostazioni, pasti riordinabili e ordine dei reparti"
```

---

## Task 17: PWA installabile e offline

**Files:**
- Create: `public/manifest.json`, `public/sw.js`, `public/icona-192.png`, `public/icona-512.png`
- Create: `src/components/RegistraSW.tsx`
- Modify: `src/app/layout.tsx`

**Interfaces:**
- Consumes: niente
- Produces: l'app installabile su Android con la lista consultabile offline

- [ ] **Step 1: Creare le icone**

Il marchio tutto pieno su fondo `#F1F0EE`: griglia 3×2 nell'ordine di `ORDINE_MARCHIO`. Generare 192px e 512px. `design/build.sh` non le produce — vanno esportate a mano dal canvas o disegnate come SVG e convertite.

- [ ] **Step 2: Scrivere il manifest**

`public/manifest.json`:

```json
{
  "name": "Spesa",
  "short_name": "Spesa",
  "start_url": "/lista",
  "display": "standalone",
  "background_color": "#F1F0EE",
  "theme_color": "#F1F0EE",
  "orientation": "portrait",
  "icons": [
    { "src": "/icona-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
    { "src": "/icona-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 3: Scrivere il service worker**

Scritto a mano invece di `next-pwa`: servono trenta righe, e una dipendenza in più per questo non si giustifica.

`public/sw.js`:

```js
const CACHE = 'spesa-v1';
const GUSCIO = ['/lista', '/settimana', '/piatti', '/manifest.json'];

self.addEventListener('install', (e) => {
  e.waitUntil(caches.open(CACHE).then((c) => c.addAll(GUSCIO)).then(() => self.skipWaiting()));
});

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys()
      .then((k) => Promise.all(k.filter((n) => n !== CACHE).map((n) => caches.delete(n))))
      .then(() => self.clients.claim()),
  );
});

self.addEventListener('fetch', (e) => {
  const url = new URL(e.request.url);
  // Le chiamate a Supabase non si mettono mai in cache: mostrerebbero dati
  // vecchi come se fossero freschi. Offline ci pensa la coda delle spunte.
  if (e.request.method !== 'GET' || url.pathname.startsWith('/rest/')) return;
  e.respondWith(
    fetch(e.request)
      .then((r) => {
        const copia = r.clone();
        caches.open(CACHE).then((c) => c.put(e.request, copia));
        return r;
      })
      .catch(() => caches.match(e.request).then((r) => r ?? caches.match('/lista'))),
  );
});
```

- [ ] **Step 4: Registrare il service worker**

`src/components/RegistraSW.tsx` è un client component con `useEffect` che chiama `navigator.serviceWorker.register('/sw.js')` solo in produzione. Montarlo in `src/app/layout.tsx`, che dichiara anche `metadata.manifest = '/manifest.json'` e `themeColor: '#F1F0EE'`.

- [ ] **Step 5: Verificare l'installazione su Android**

```bash
npm run build && npm start
```

Aprire da Chrome su Android (via rete locale o deploy Vercel di prova).
Expected: compare il prompt di installazione; installata, si apre senza barra del browser; in modalità aereo la schermata lista si apre e le spunte funzionano.

- [ ] **Step 6: Commit**

```bash
git add public src/components/RegistraSW.tsx src/app/layout.tsx
git commit -m "feat: PWA installabile con guscio offline"
```

---

## Task 18: Rifinitura, verifica della spec e deploy

**Files:**
- Modify: `README.md`
- Create: `.github/workflows/test.yml` (opzionale)

- [ ] **Step 1: Eseguire tutta la suite**

Run: `npm test && npx tsc --noEmit && npm run build`
Expected: tutto verde. Nessun `any` implicito, nessun warning di build.

- [ ] **Step 2: Verificare i confini architetturali**

```bash
grep -rE "from '@?/?(next|react|@supabase)" src/domain/ && echo "VIOLAZIONE" || echo "dominio pulito"
grep -rn "costruisciLista\|nuovoResiduo\|calcolaChiusura" src/app/ && echo "ARITMETICA NELLA UI" || echo "ui pulita"
```

Expected: `dominio pulito` e `ui pulita`. `costruisciLista` va chiamata solo da `src/data/lista.ts`.

- [ ] **Step 3: Passare la spec riga per riga**

Aprire `docs/superpowers/specs/2026-08-26-spesa-design.md` e verificare a mano:

- [ ] nessuna schermata accetta un residuo come input
- [ ] nessuna chiamata LLM, nessuna tabella `correction`
- [ ] `kcal_100` e sorelle esistono e non sono lette da nessuna parte
- [ ] i pasti vanno da 3 a 5 e nessuna schermata assume quattro colonne
- [ ] il marchio non ha mai una casella grigia
- [ ] la freccia del selettore settimana è presente e inerte
- [ ] le settimane passate restano salvate, non cancellate
- [ ] nessun prodotto non alimentare è inseribile (le sei aree sono l'unico vincolo, ed è sufficiente)

- [ ] **Step 4: Deploy su Vercel**

```bash
npx vercel --prod
```

Impostare `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY` nelle variabili d'ambiente del progetto. Aggiungere l'URL di produzione alle Redirect URL di Supabase Auth, altrimenti il magic link rimanda a localhost.

- [ ] **Step 5: Aggiornare il README**

Sostituire «Stato: design della v1 chiuso. Nessuna riga di codice applicativo scritta» con lo stato reale, l'URL di produzione e le istruzioni per lo sviluppo locale. Aggiungere la riga sul gate: **tre settimane di uso reale prima di aprire la Fase 2.**

- [ ] **Step 6: Commit e tag**

```bash
git add -A
git commit -m "chore: Fase 1 completa, deploy e README aggiornato"
git tag fase-1
```

---

## Il gate

Fase 1 finisce qui. **Non iniziare la Fase 2.**

La spec impone tre settimane di uso reale prima di costruire altro: se la Fase 1 non viene aperta per tre settimane consecutive, le fasi successive automatizzerebbero un rituale che non esiste. Il criterio di successo dichiarato è misurabile, e va misurato davvero:

> Per tre settimane consecutive la lista viene generata, usata al supermercato, e la spesa serale per il singolo giorno non avviene più di una volta a settimana.

Cosa guardare dopo tre settimane, in ordine di importanza:

1. **Il residuo è rimasto vero?** Confrontare `pantry_state.residuo` con quello che c'è davvero in dispensa su cinque ingredienti porzionabili. Se diverge molto, il modello del residuo ha un buco e nessuna fase successiva lo aggiusterà.
2. **Il check-in viene fatto?** Se le settimane restano in stato `bozza`, il problema è l'attrito del check-in, e la Fase 2 (calendario) è la risposta giusta.
3. **Il repertorio è invecchiato?** Se i piatti reali non sono più quelli inseriti, la risposta è la Fase 3 (correzioni), non altra UI.
